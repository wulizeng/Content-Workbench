# 产品需求文档（PRD）：店铺岗位管理

| 项目 | 内容 |
|------|------|
| **产品名称** | 店铺岗位管理 |
| **所属模块** | 店铺管理 |
| **版本** | V1.1 |
| **文档状态** | 可落地开发 |
| **创建日期** | 2026-08-01 |
| **最近更新** | 2026-09-02 |

---

## 一、更新记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|----------|--------|
| V1.0 | 2026-08-01 | 初始版本：岗位克隆（新增）与岗位权限同步（修改）双操作统一入口、列表页查询与管理、新增/修改创建页、新增详情页与同步详情页、后台检查结果、权限对比预览 | 产品部 |
| V1.0.1 | 2026-08-20 | 原「店铺岗位克隆」「岗位权限同步」两个独立菜单合并为「店铺岗位管理」单一菜单入口，通过列表页「操作类型」列与新建下拉菜单（新增/修改）区分两类操作 | 产品部 |
| V1.1 | 2026-09-02 | ①列表页「类型」列更名为「操作类型」，列位置调整至「源店铺名称」之后、「源岗位」之前；②克隆/同步执行按钮文案统一为「确认执行」（取消「确认执行克隆」「确认执行同步」带后缀文案）；③流程说明区文案规范化（动作主语明确为用户/系统） | 产品部 |

---

## 二、需求背景

### 2.1 业务背景

内容电商多平台（抖音/快手/小红书）多店铺运营体系下，各店铺需要在平台后台配置岗位及对应的操作权限（营销工具、推广工具、商品管理、订单管理、数据分析、客服管理等 6 大分类、数十项权限点）。

实际业务中存在两类高频诉求：

1. **批量建岗（新增）**：旗舰店按标准岗位模型（如运营主管、商品运营、客服专员）配置完成后，同平台新开店、并购店需要重复搭建相同岗位体系。逐店铺手工配置 1 个岗位约 68 项权限，操作繁琐、易漏配错配。
2. **权限对齐（修改）**：源店铺岗位权限模型迭代后（如新增「达人专属营销」权限），其他店铺同职责岗位需要同步对齐，人工逐项比对差异效率低且不可追溯。

### 2.2 痛点分析

| 痛点 | 描述 |
|------|------|
| 重复劳动 | 每个新店铺需重复配置全量岗位与权限，单店铺单岗位配置成本高 |
| 配置不一致 | 人工配置易出现店铺间权限差异，导致运营动作在部分店铺不可执行 |
| 无差异视图 | 权限对齐缺少「源 vs 目标」差异对比，人工核对困难 |
| 无记录可溯 | 操作完成后无执行记录、无检查结论，出问题无法回溯定位 |

### 2.3 产品目标与收益

- 提供店铺岗位**统一管理入口**，以「新增（克隆）/修改（同步）」两种操作类型覆盖建岗与对齐两类场景
- 克隆支持**一对多**（1 个源店铺 + N 个目标店铺），同步支持**一对一**（源岗位 → 目标岗位）
- 通过**同平台约束**与**岗位冲突校验**保证操作合法性
- 执行后由系统自动完成**后台检查**，输出检查结论；同步提供**权限差异对比预览**，先预览后执行
- 全部操作形成**执行记录**，支持查询、详情下钻、删除，实现全生命周期可追溯

### 2.4 预期收益

| 指标 | 现状 | 目标 |
|------|------|------|
| 单店铺岗位体系搭建耗时 | 约 30 分钟/店铺（人工） | ≤ 1 分钟（系统执行） |
| 店铺间岗位权限不一致率 | 偶发、难发现 | 后台检查 100% 覆盖 |
| 权限对齐操作可追溯性 | 无记录 | 记录 + 明细 + 日志全链路留痕 |

---

## 三、业务流程

### 3.1 功能全景流程

```
店铺岗位管理（列表页）
│
├── 查询/重置：源店铺 / 目标店铺 / 状态 / 创建时间
│
├── + 新建 ▾（下拉）
│   ├── 新增（岗位克隆）──→ 新增创建页 ──→ 确认执行 ──→ 后台检查结果 ──→ 结果页 ──→ 记录入列表
│   └── 修改（权限同步）──→ 修改创建页 ──→ 确认执行 ──→ 权限对比预览 ──→ 确认同步 ──→ 结果页 ──→ 记录入列表
│
├── 查看详情
│   ├── 新增记录 ──→ 新增详情页（基本信息 / 新增明细 / 操作日志）
│   └── 修改记录 ──→ 同步详情页（基本信息 / 权限变更明细 / 操作日志，支持再次确认执行）
│
└── 删除记录（二次确认）
```

### 3.2 新增（岗位克隆）流程

```
用户进入「店铺岗位管理」→ 点击「+ 新建 ▾」→ 选择「新增」
    ↓
打开新增创建页（系统内新页签「新建岗位克隆」）
    ↓
第一步：选择要克隆岗位的源店铺
  • 按店铺名称 / 店铺编码 / 平台关键字搜索
  • 单选店铺卡片（展示店铺名称、店铺编码、N 个岗位可克隆）
    ↓（系统自动加载源店铺全部岗位）
第二步：选择需要克隆的岗位（多选，支持全选）
  • 岗位卡片展示：岗位名称 / 职责描述 / 权限项数
    ↓
第三步：选择需要克隆到的目标店铺（可多选）
  • 仅展示与源店铺同平台的店铺，自动排除源店铺自身
  • 支持关键字筛选
    ↓
底部确认栏实时汇总：源店铺 / 岗位 N 个 / 权限 N 项 / 目标店铺 N 个
    ↓ 用户点击「确认执行」（岗位与目标店铺均非空时按钮可用）
系统执行克隆 → 展示后台检查结果
  • 按目标店铺逐个输出检查项：岗位「X」已创建 / 岗位权限与源店铺一致 / 岗位人员绑定关系正常
    ↓
结果页：岗位克隆成功（展示源店铺 / 克隆岗位 / 权限总数 / 目标店铺 / 克隆编号 / 后台检查结论）
    ↓
「继续克隆」重置本页再次操作，或「返回列表」
```

### 3.3 修改（岗位权限同步）流程

```
用户进入「店铺岗位管理」→ 点击「+ 新建 ▾」→ 选择「修改」
    ↓
打开修改创建页（系统内新页签「新建岗位权限同步」）
    ↓
第一步：选择权限模板的源店铺（搜索，单选）
    ↓（系统自动加载源店铺全部岗位）
第二步：选择源岗位（权限模板，单选）
    ↓
第三步：选择要修改权限的目标店铺与岗位
  • 目标店铺：同平台、排除源店铺，单选
  • 系统自动加载目标店铺已有岗位 → 选择目标岗位（单选）
  • 目标店铺无岗位时提示：请先使用「店铺岗位克隆」功能创建岗位
    ↓
底部确认栏汇总：源：店铺 · 岗位 → 目标：店铺 · 岗位
    ↓ 用户点击「确认执行」（四项均选择后按钮可用）
系统生成权限对比预览
  • 按权限分类展示：源岗位权限 / 目标岗位权限
  • 差异标记：将新增（源有目标无，绿）/ 一致（灰）/ 将移除（目标有源无，红）
  • 汇总：新增 N 项 / 移除 M 项
    ↓ 用户点击「确认同步」（或「返回修改」重新选择）
系统执行同步 → 结果页：岗位权限同步成功（源岗位 / 目标岗位 / 新增 / 移除 / 同步编号）
    ↓
「继续同步」重置本页再次操作，或「返回列表」
```

### 3.4 执行状态流转

```
创建记录 → 待执行 → 执行中 → 全部成功 / 部分成功 / 全部失败
```

| 枚举值 | 标签样式 | 说明 |
|--------|---------|------|
| 待执行 | 黄色 | 记录已创建，等待执行 |
| 执行中 | 蓝色 | 正在执行克隆/同步操作 |
| 全部成功 | 绿色 | 全部目标店铺/岗位执行成功 |
| 部分成功 | 橙色 | 存在成功与失败并存（如部分权限冲突跳过） |
| 全部失败 | 红色 | 全部执行失败（如同名岗位冲突终止），记录失败原因 |

---

## 四、需求描述

### 4.1 名词解释

| 名词 | 定义 |
|------|------|
| 源店铺 | 作为岗位/权限模板来源的店铺 |
| 源岗位 | 源店铺下被克隆或作为权限模板的岗位；克隆时可多选，同步时单选 |
| 目标店铺 | 接收克隆岗位或被修改岗位权限的店铺；与源店铺必须同平台，且不能是源店铺自身 |
| 目标岗位 | 同步场景下被修改权限的岗位，来自目标店铺已有岗位 |
| 新增（克隆） | 将源店铺选中岗位及其全量权限复制创建到目标店铺的操作 |
| 修改（同步） | 将源岗位权限配置对齐到目标岗位的操作，产生新增/移除两类权限变更 |
| 克隆编号/同步编号 | 记录唯一标识，格式：前缀 + 年月日 + 4 位流水（CL=克隆，SY=同步），如 CL20260801001 |
| 后台检查 | 克隆执行完成后，系统自动在店铺后台校验岗位创建、权限配置、人员绑定关系的过程 |

### 4.2 功能架构

```
店铺岗位管理
├── 页面标题区（店铺岗位管理）
├── 流程说明区（5 步横向流程卡片）
├── 查询区域（源店铺 / 目标店铺 / 状态 / 创建时间 + 查询/重置 + 条件标签）
├── 工具栏（+ 新建 ▾ 下拉：新增 / 修改）
├── 记录列表（15 列，分页，按创建时间倒序）
├── 新增创建页（三区域单页：源店铺 → 克隆岗位 → 目标店铺 + 确认栏）
├── 修改创建页（四区域单页：源店铺 → 源岗位 → 目标店铺 → 目标岗位 + 确认栏）
├── 新增详情页（基本信息 / 新增明细 / 操作日志）
└── 同步详情页（基本信息 / 权限变更明细 / 操作日志 + 确认执行）
```

### 4.3 页面清单

| 页面 | 页面路径 | 系统内页签标题 | 打开方式 |
|------|---------|--------------|---------|
| 记录列表页 | pages/store-engine/position-clone-list.html | 店铺岗位管理 | 侧边栏菜单「店铺管理 / 店铺岗位管理」 |
| 新增创建页 | pages/store-engine/position-clone.html | 新建岗位克隆 | 列表页「+ 新建 ▾ → 新增」 |
| 修改创建页 | pages/store-engine/position-sync.html | 新建岗位权限同步 | 列表页「+ 新建 ▾ → 修改」 |
| 新增详情页 | pages/store-engine/position-clone-detail.html?id={编号} | 克隆详情 - {编号} | 列表页新增记录「查看详情」 |
| 同步详情页 | pages/store-engine/position-sync-detail.html?id={编号} | 同步详情 - {编号} | 列表页修改记录「查看详情」 |

### 4.4 通用规则

- **页签机制**：所有子页面通过系统内页签打开（postMessage openTab），页内「返回列表/返回」通过 closeTab 关闭当前页签；独立访问时降级为 window.open / window.close
- **面包屑**：列表页为「店铺管理 / 店铺岗位管理」；创建页分别为「店铺管理 / 店铺岗位克隆」「店铺管理 / 岗位权限同步」
- **同平台约束**：目标店铺候选集 = 与源店铺同平台的全部店铺 − 源店铺自身
- **记录合并**：克隆与同步记录合并展示于同一列表，按创建时间倒序，以「操作类型」列区分
- **空值显示**：无值字段显示「-」；列表无数据显示空状态「暂无记录」
- **状态列显示**：所有视图状态仅显示中文文本标签，不使用图标

---

## 五、需求列表

| 编号 | 需求名称 | 需求描述 | 优先级 | 所属页面 |
|------|---------|---------|--------|---------|
| PZM-001 | 流程说明区 | 以 5 步横向卡片展示功能闭环：选择源店铺 → 选择克隆岗位 → 选择目标店铺 → 确认执行 → 检查结果 | P1 | 列表页 |
| PZM-002 | 记录查询 | 支持源店铺、目标店铺、状态、创建时间区间四项条件查询；支持重置；已选条件以标签展示且可单个移除 | P0 | 列表页 |
| PZM-003 | 新建入口 | 「+ 新建 ▾」下拉菜单提供「新增」「修改」两个操作类型入口，分别打开对应创建页签 | P0 | 列表页 |
| PZM-004 | 记录列表展示 | 15 列表格展示全部克隆/同步记录；操作类型以胶囊标签（新增蓝/修改橙）区分；状态以 5 枚举彩色标签展示；分页展示 | P0 | 列表页 |
| PZM-005 | 查看详情 | 按记录操作类型跳转新增详情页或同步详情页（系统内新页签） | P0 | 列表页 |
| PZM-006 | 删除记录 | 删除单条记录，需二次确认，成功后 Toast 提示并刷新列表 | P1 | 列表页 |
| PZM-007 | 新增-选择源店铺 | 关键字搜索（店铺名称/店铺编码/平台）+ 卡片单选；选中后自动加载岗位与目标店铺 | P0 | 新增创建页 |
| PZM-008 | 新增-选择克隆岗位 | 岗位多选，支持全选/取消全选，实时展示已选计数；展示岗位名称、职责描述、权限项数 | P0 | 新增创建页 |
| PZM-009 | 新增-选择目标店铺 | 同平台候选、排除源店铺、可多选、支持关键字筛选与计数展示 | P0 | 新增创建页 |
| PZM-010 | 新增-确认执行 | 底部确认栏实时汇总（源店铺/岗位数/权限数/目标店铺数）；岗位与目标店铺均非空时「确认执行」按钮可用 | P0 | 新增创建页 |
| PZM-011 | 新增-后台检查结果 | 执行后展示三步进度条与检查面板：按目标店铺输出检查项与结论标签 | P0 | 新增创建页 |
| PZM-012 | 新增-结果页 | 克隆成功结果摘要（源店铺/克隆岗位/权限总数/目标店铺/克隆编号/后台检查结论）；提供「继续克隆」「返回列表」 | P0 | 新增创建页 |
| PZM-013 | 修改-选择源店铺与源岗位 | 源店铺搜索单选；源岗位作为权限模板单选 | P0 | 修改创建页 |
| PZM-014 | 修改-选择目标店铺与目标岗位 | 目标店铺同平台单选；联动加载目标店铺已有岗位并单选；无岗位时给出克隆引导提示 | P0 | 修改创建页 |
| PZM-015 | 修改-权限对比预览 | 按权限分类对比源/目标岗位权限，差异三色标记并统计新增/移除项数；支持「确认同步」与「返回修改」 | P0 | 修改创建页 |
| PZM-016 | 修改-结果页 | 同步成功结果摘要；提供「继续同步」「返回列表」 | P0 | 修改创建页 |
| PZM-017 | 新增详情页 | 三区块：基本信息（一行 4 列）/ 新增明细表 / 操作日志时间轴 | P1 | 新增详情页 |
| PZM-018 | 同步详情页 | 三区块：基本信息 / 权限变更明细表 / 操作日志时间轴；页头提供「确认执行」支持再次执行 | P1 | 同步详情页 |
| PZM-019 | 记录编号规则 | 克隆编号前缀 CL、同步编号前缀 SY，编号全局唯一 | P0 | 通用 |
| PZM-020 | 执行状态枚举 | 待执行/执行中/全部成功/部分成功/全部失败 五枚举及统一标签样式 | P0 | 通用 |

---

## 六、需求详情

### 6.1 记录列表页

**页面路径**：`pages/store-engine/position-clone-list.html`
**侧边栏菜单**：店铺管理 / 店铺岗位管理

#### 6.1.1 流程说明区（PZM-001）

渐变底色卡片内横向展示 5 个步骤节点（图标 + 标题 + 说明），节点间以箭头连接：

| 步骤 | 标题 | 说明 |
|------|------|------|
| 1 | 选择源店铺 | 用户选择要克隆的源店铺，自动获取所有岗位 |
| 2 | 选择克隆岗位 | 用户选择需要克隆复制的岗位 |
| 3 | 选择目标店铺 | 用户选择需要克隆到的目标店铺 |
| 4 | 确认执行 | 系统自动后执行克隆修改操作 |
| 5 | 检查结果 | 系统自动检查克隆结果 |

#### 6.1.2 查询区域（PZM-002）

| 查询项 | 类型 | 匹配规则 |
|--------|------|----------|
| 源店铺 | 下拉选择 | 全部 + 店铺名称列表，精确匹配 |
| 目标店铺 | 下拉选择 | 全部 + 店铺名称列表，克隆记录命中任一目标店铺即返回；同步记录精确匹配 |
| 状态 | 下拉选择 | 全部 / 待执行 / 执行中 / 全部成功 / 部分成功 / 全部失败，精确匹配 |
| 创建时间 | 日期范围 | 起止日期区间过滤（yyyy-MM-dd，含结束日全天） |

- 「查询」按条件过滤并展示条件标签；「重置」清空全部条件并恢复全量
- 条件标签展示于查询区下方，点击「×」可移除单个条件并即时刷新

#### 6.1.3 工具栏（PZM-003）

| 元素 | 说明 |
|------|------|
| + 新建 ▾ | 主按钮，点击展开下拉菜单；点击页面其他区域自动收起 |
| 下拉-新增 📋 | 打开「新建岗位克隆」页签（新增创建页） |
| 下拉-修改 🔄 | 打开「新建岗位权限同步」页签（修改创建页） |

#### 6.1.4 记录列表（PZM-004）

| 列名 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 序号 | — | 数字 | 自增序号 |
| 编号 | id | 文本 | 等宽字体、主题色高亮；克隆 CL / 同步 SY 前缀 |
| 平台 | platform | 标签 | 抖音 / 快手 / 小红书，紫色胶囊标签 |
| 源店铺编码 | sourceShopId | 文本 | 等宽字体（如 dy_100231） |
| 源店铺名称 | sourceStore | 文本 | — |
| 操作类型 | type | 标签 | 新增（蓝色胶囊）/ 修改（橙色胶囊） |
| 源岗位 | positions / sourcePosition | 标签组/文本 | 新增记录：多岗位胶囊标签组；修改记录：单岗位文本 |
| 目标岗位 | positions / targetPosition | 标签组/文本 | 新增记录：与源岗位相同（克隆）；修改记录：单岗位文本 |
| 目标店铺 | targetStores / targetStore | 标签组/文本 | 新增记录：多店铺绿色标签组（悬浮展示店铺编码）；修改记录：单店铺标签 |
| 岗位数 | totalPositions | 数字 | 记录涉及岗位数量 |
| 权限总数 | totalPerms | 数字 | 记录涉及权限项总数 |
| 状态 | status | 状态标签 | 5 枚举，见 3.4 |
| 创建人 | operator | 文本 | — |
| 创建时间 | createTime | 日期时间 | yyyy-MM-dd HH:mm:ss |
| 操作 | — | 链接 | 查看详情 / 删除 |

**操作列规则**：
- **查看详情**：始终显示；新增记录 → 新增详情页，修改记录 → 同步详情页
- **删除**：始终显示；点击二次确认「确定要删除该记录吗？」，确认后移除记录，Toast「删除成功」

**排序与分页**：按创建时间倒序；底部分页条展示「共 N 条」与页码。

**空状态**：图标 + 「暂无记录」+ 引导文案「点击『+ 新建』创建记录」。

### 6.2 新增创建页（岗位克隆）

**页面路径**：`pages/store-engine/position-clone.html`（页内标题「店铺岗位新增」）

#### 6.2.1 第一步：选择要克隆岗位的源店铺（PZM-007）

- 搜索栏支持店铺名称 / 店铺编码 / 平台关键字模糊匹配；未输入关键字时展示占位提示「请输入店铺名称或店铺ID搜索选择源店铺」
- 命中结果以卡片网格展示：店铺名称、店铺编码（dy_100231 形式）、「N 个岗位可克隆」；展示匹配计数「X / Y 个店铺」与「清除」入口
- 单选；选中后自动加载第二步岗位列表与第三步目标店铺
- 无匹配时提示「未找到匹配的店铺」；清除搜索时联动重置岗位、目标店铺与确认栏

#### 6.2.2 第二步：选择需要克隆的岗位（PZM-008）

- 选中源店铺后自动加载其全部岗位，面板提示「已加载『XX』的岗位」
- 每个岗位卡片：复选框 + 岗位名称 + 职责描述 + 「N 项权限」徽标
- 顶部工具条：全选复选框 + 「已选 X / Y 个岗位」实时计数
- 源店铺无岗位时提示「该店铺暂无岗位，请选择其他店铺」

#### 6.2.3 第三步：选择需要克隆到的目标店铺（PZM-009）

- 候选集 = 与源店铺同平台店铺 − 源店铺自身；卡片网格多选，选中态右上角显示对勾标记
- 内置筛选栏（店铺名称/店铺编码），计数展示「X / Y 个店铺（平台名）」
- 当前平台无其他店铺时提示「当前平台无其他可选店铺」

#### 6.2.4 确认执行（PZM-010）

- 确认栏随选择实时出现，汇总展示：源店铺 / 岗位 N 个 / 权限 N 项 / 目标店铺 N 个
- 「确认执行」按钮：岗位与目标店铺均非空时可用，否则禁用
- 任一关键项缺失时执行给出警示「请完成所有选择后再执行」

#### 6.2.5 后台检查结果（PZM-011）

- 执行后隐藏选择区域，展示三步进度条：选择配置 ✓ → 确认执行 ✓ → 后台检查结果（进行中）
- 检查面板先展示加载态「正在检查店铺后台岗位权限配置...」，随后按目标店铺分组输出：
  - 店铺级状态标签：检查通过（通过/警告/失败三态样式）
  - 检查项：岗位「X」已创建（N 项权限已配置）、岗位权限与源店铺一致（权限比对通过）、岗位人员绑定关系正常（无冲突）
- 面板底部汇总条：全部检查通过 + 克隆编号

#### 6.2.6 结果页（PZM-012）

- 标题「岗位克隆成功」，说明文案「已将源店铺岗位及权限配置克隆到目标店铺，后台检查全部通过」
- 摘要明细：源店铺 / 克隆岗位（多岗位顿号连接）/ 权限总数 / 目标店铺 / 克隆编号 / 后台检查：全部通过
- 操作按钮：「继续克隆」（重置本页所有选择与筛选）/「返回列表」（关闭页签）

### 6.3 修改创建页（岗位权限同步）

**页面路径**：`pages/store-engine/position-sync.html`（页内标题「岗位权限同步」）

#### 6.3.1 第一步 + 第二步：选择源店铺与源岗位（PZM-013）

- 源店铺搜索单选（同 6.2.1 交互）
- 源岗位作为权限模板单选；选中后联动加载第三步目标区域

#### 6.3.2 第三步：选择要修改权限的目标店铺与岗位（PZM-014）

- 目标店铺：同平台、排除源店铺，**单选**，卡片网格
- 选中目标店铺后自动加载其已有岗位列表（单选），提示「『XX』已有 N 个岗位」
- 目标店铺无岗位时提示「该店铺暂无岗位，无法进行权限同步 — 请先使用『店铺岗位克隆』功能创建岗位」

#### 6.3.3 确认执行与权限对比预览（PZM-015）

- 确认栏：`源：店铺 · 岗位 → 目标：店铺 · 岗位`；四项均选择后「确认执行」可用
- 点击「确认执行」后进入**权限对比预览**（隐藏选择区域）：
  - 表格列：权限分类 / 源岗位（X）/ 目标岗位（Y）
  - 权限项三态标记：源有目标无 → 绿色（将新增）；双方一致 → 灰色；目标有源无 → 红色（将移除）
  - 汇总统计：新增 N 项 / 移除 M 项
- 底部按钮：「确认同步」（执行变更）/「返回修改」（回到选择区域，保留已选状态）

#### 6.3.4 结果页（PZM-016）

- 标题「岗位权限同步成功」，说明文案「已将源岗位权限配置同步到目标岗位，后台更新完成」
- 摘要明细：源岗位 / 目标岗位 / 新增权限 / 移除权限 / 同步编号
- 操作按钮：「继续同步」/「返回列表」

### 6.4 新增详情页

**页面路径**：`pages/store-engine/position-clone-detail.html?id={新增编号}`（PZM-017）

**页头**：「新增详情」+ 编号胶囊 + 返回按钮。

#### 6.4.1 基本信息

一行 4 列网格，字段以「字段名称：字段值」平铺展示：

| 字段 | 说明 |
|------|------|
| 新增编号 | 等宽字体胶囊 |
| 平台 | 抖音 / 快手 / 小红书 |
| 源店铺 | 店铺编码 + 店铺名称 |
| 执行状态 | 5 枚举状态标签 |
| 创建人 | — |
| 新增岗位数 | N 个 |
| 权限总数 | N 项 |
| 目标店铺数 | N 个 |
| 创建时间 | yyyy-MM-dd HH:mm:ss |

#### 6.4.2 新增明细

区块头部展示明细总数徽标，表格逐行展示「目标店铺 × 源岗位」粒度执行结果：

| 列名 | 说明 |
|------|------|
| 目标店铺编码 | 等宽字体 |
| 目标店铺名称 | — |
| 源岗位名称 | — |
| 权限数 | 该岗位权限项数 |
| 源岗位权限ID | 源岗位权限标识串 |
| 状态 | 行级执行状态标签 |
| 失败原因 | 红色字体，无值显示「-」（如：目标店铺存在同名岗位冲突，已跳过 N 项） |

#### 6.4.3 操作日志

流水式时间轴（动作 / 时间 / 详情），节点示例：创建克隆任务（选择源店铺「XX」，N 个岗位，目标店铺 M 个）→ 开始执行克隆 → 克隆完成（成功克隆 N 个岗位、M 项权限到 K 个目标店铺）→ 后台检查通过。

### 6.5 同步详情页

**页面路径**：`pages/store-engine/position-sync-detail.html?id={同步编号}`（PZM-018）

**页头**：「同步详情」+ 编号胶囊 + **确认执行**按钮（支持在详情页再次发起同步）+ 返回按钮。

#### 6.5.1 基本信息

一行 4 列网格：

| 字段 | 说明 |
|------|------|
| 同步编号 | 等宽字体胶囊 |
| 平台 | 抖音 / 快手 / 小红书 |
| 源店铺 | 店铺编码 + 店铺名称 |
| 源岗位 | 权限模板岗位 |
| 目标店铺 | 店铺编码 + 店铺名称 |
| 目标岗位 | 被修改权限的岗位 |
| 执行状态 | 5 枚举状态标签 |
| 创建人 | — |
| 新增权限 | 绿色「N 项」 |
| 移除权限 | 红色「M 项」 |
| 创建时间 | yyyy-MM-dd HH:mm:ss |

#### 6.5.2 权限变更明细

| 列名 | 说明 |
|------|------|
| 序号 | 自增 |
| 源岗位 / 源岗位权限ID | 变更基准 |
| 目标店铺编码 / 目标店铺名称 | 变更对象所属店铺 |
| 目标岗位 / 目标岗位权限ID | 变更对象岗位及其原权限标识 |
| 修改后目标岗位权限ID | 同步完成后的权限标识 |
| 状态 | 行级执行状态标签 |
| 失败原因 | 红色字体，无值显示「-」 |

#### 6.5.3 操作日志

时间轴节点示例：创建同步任务（源：店铺 · 岗位 → 目标：店铺 · 岗位）→ 开始执行同步 → 同步完成（新增 N 项权限，移除 M 项权限）→ 后台更新完成。

### 6.6 编号与状态规则（PZM-019 / PZM-020）

| 项目 | 规则 |
|------|------|
| 编号格式 | 2 位前缀 + YYYYMMDD + 4 位流水号，如 `CL20260801001`、`SY20260810001` |
| 前缀映射 | CL=新增（克隆），SY=修改（同步） |
| 流水号规则 | 同日同前缀下自增，从 0001 开始补零至 4 位 |
| 唯一性 | 编号全局唯一，作为详情页定位参数与删除主键 |

---

## 七、数据埋点

### 7.1 通用属性

所有埋点事件均携带以下公共属性：

| 属性 | 说明 |
|------|------|
| user_id | 用户唯一标识 |
| user_role | 用户角色（运营专员/运营主管/超级管理员） |
| page | 页面标识（position_clone_list / position_clone / position_sync / position_clone_detail / position_sync_detail） |
| record_id | 记录编号（涉及具体记录的事件携带） |

### 7.2 事件定义

| 事件ID | 事件名称 | 触发时机 | 专属属性 |
|--------|---------|---------|---------|
| pzm_list_view | 列表页浏览 | 列表页加载完成 | record_total（记录总数） |
| pzm_list_search | 列表查询 | 点击「查询」 | source_store / target_store / status / date_from / date_to / result_count |
| pzm_list_reset | 列表重置 | 点击「重置」 | — |
| pzm_list_tag_remove | 移除条件标签 | 点击条件标签「×」 | tag_field（被移除的条件项） |
| pzm_list_new_menu_open | 新建菜单展开 | 点击「+ 新建 ▾」 | — |
| pzm_list_new_click | 新建入口点击 | 点击下拉「新增」或「修改」 | op_type（新增/修改） |
| pzm_list_detail_click | 查看详情点击 | 点击「查看详情」 | op_type / record_id |
| pzm_list_delete_click | 删除点击 | 点击「删除」 | record_id |
| pzm_list_delete_confirm | 删除确认 | 二次确认弹窗点击「确定」 | record_id |
| pzm_clone_view | 新增创建页浏览 | 页面加载完成 | — |
| pzm_clone_source_search | 源店铺搜索 | 源店铺搜索输入（防抖 500ms） | keyword / result_count |
| pzm_clone_source_select | 源店铺选中 | 点击源店铺卡片 | store_id / position_total（可克隆岗位数） |
| pzm_clone_position_select | 克隆岗位选择 | 岗位勾选变化 | selected_count / total_count |
| pzm_clone_target_select | 目标店铺选择 | 目标店铺勾选变化 | selected_count |
| pzm_clone_execute_click | 确认执行点击 | 点击「确认执行」 | store_id / position_ids / position_count / perm_total / target_count |
| pzm_clone_check_done | 后台检查完成 | 检查结果渲染完成 | record_id / pass_store_count |
| pzm_clone_result_view | 克隆结果页浏览 | 结果页展示 | record_id / status |
| pzm_clone_continue | 继续克隆点击 | 结果页点击「继续克隆」 | — |
| pzm_sync_view | 修改创建页浏览 | 页面加载完成 | — |
| pzm_sync_source_select | 源岗位选中 | 点击源岗位卡片 | store_id / position_id |
| pzm_sync_target_select | 目标岗位选中 | 点击目标岗位卡片 | target_store_id / target_position_id |
| pzm_sync_execute_click | 确认执行点击 | 点击「确认执行」进入对比预览 | add_count / remove_count（预览差异统计） |
| pzm_sync_compare_confirm | 确认同步点击 | 对比预览页点击「确认同步」 | add_count / remove_count |
| pzm_sync_compare_back | 返回修改点击 | 对比预览页点击「返回修改」 | — |
| pzm_sync_result_view | 同步结果页浏览 | 结果页展示 | record_id / status |
| pzm_clone_detail_view | 新增详情浏览 | 详情页加载完成 | record_id / status / detail_count |
| pzm_sync_detail_view | 同步详情浏览 | 详情页加载完成 | record_id / status / add_count / remove_count |
| pzm_sync_detail_execute | 详情页再次执行 | 同步详情页点击「确认执行」 | record_id |

### 7.3 埋点说明

- 事件命名遵循 `pzm_` 前缀（Position Manage Module，店铺岗位管理模块缩写），蛇形命名，页面内动作按「对象 + 动作」组织
- 删除类、执行类操作同时采集点击与确认两个事件，用于漏斗转化分析
- 执行类事件（execute/confirm）必须携带操作对象集合摘要属性，用于成功率与规模分析

---

## 八、权限需求

### 8.1 角色定义

| 角色 | 说明 |
|------|------|
| 运营专员 | 店铺日常执行人员，负责本团队店铺的岗位维护 |
| 运营主管 | 团队管理角色，负责多店铺岗位模型治理 |
| 超级管理员 | 平台管理员，全量店铺与记录管理 |

### 8.2 菜单权限

| 角色 | 「店铺管理 / 店铺岗位管理」菜单可见性 |
|------|--------------------------------------|
| 运营专员 | ✅（仅授权店铺范围） |
| 运营主管 | ✅ |
| 超级管理员 | ✅ |

### 8.3 操作权限矩阵

| 操作 | 运营专员 | 运营主管 | 超级管理员 |
|------|:-------:|:-------:|:---------:|
| 记录查询（PZM-002） | ✅ | ✅ | ✅ |
| 新建-新增（克隆）（PZM-003/007~012） | ✅ | ✅ | ✅ |
| 新建-修改（同步）（PZM-013~016） | ✅ | ✅ | ✅ |
| 查看详情（PZM-005/017/018） | ✅ | ✅ | ✅ |
| 删除记录（PZM-006） | ❌ | ✅ | ✅ |
| 同步详情页再次执行（PZM-018） | ❌ | ✅ | ✅ |

### 8.4 数据权限

| 维度 | 规则 |
|------|------|
| 店铺范围 | 运营专员仅可选择/操作其被授权的店铺（源店铺与目标店铺候选集均按授权范围过滤）；运营主管与超级管理员覆盖全部店铺 |
| 记录范围 | 运营专员默认可见本人创建记录；运营主管可见本团队记录；超级管理员可见全部记录 |
| 平台约束 | 目标店铺必须与源店铺同平台（业务规则，非权限项），系统在候选集中强制过滤 |

### 8.5 权限校验规则

- 删除与再次执行按钮对无权限角色**不渲染**（而非置灰），避免无效引导
- 前端权限仅为展示控制，删除/执行接口须做服务端二次鉴权（角色 + 店铺数据范围）
- 同平台约束与「目标店铺 ≠ 源店铺」校验在前端与服务端双重校验，服务端为准

---

## 附录：异常与提示文案

| 场景 | 提示文案 | 处理方式 |
|------|---------|---------|
| 未完成选择即执行（新增） | 请完成所有选择后再执行 | 警告 Toast，阻断执行 |
| 源店铺搜索无匹配 | 未找到匹配的店铺 | 空态占位 |
| 源店铺无岗位 | 该店铺暂无岗位，请选择其他店铺 | 空态占位，岗位/目标区域复位 |
| 平台无其他可选店铺 | 当前平台无其他可选店铺 | 空态占位 |
| 目标店铺无岗位（同步） | 该店铺暂无岗位，无法进行权限同步，请先使用「店铺岗位克隆」功能创建岗位 | 空态占位 + 功能引导 |
| 删除确认 | 确定要删除该记录吗？ | 二次确认弹窗 |
| 删除成功 | 删除成功 | 成功 Toast |
| 克隆完成 | 岗位克隆成功，后台检查全部通过！ | 成功 Toast + 结果页 |
| 同名岗位冲突（全部失败） | 目标店铺「XX」存在同名岗位「YY」，冲突终止 | 记录状态=全部失败，日志与失败原因留痕 |
| 部分权限冲突（部分成功） | 「XX」克隆成功；「YY」部分权限冲突，已跳过 N 项 | 记录状态=部分成功 |
