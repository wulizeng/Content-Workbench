# 产品需求文档（PRD）：分销枢纽 · 客户入驻域

| 项目 | 内容 |
|------|------|
| **产品名称** | 分销枢纽 - 客户入驻域（客户注册 / 客户入驻申请 / 账号信息录入 / 客户注册账号 / 客户合作管理） |
| **所属模块** | 分销枢纽 |
| **版本** | V1.3 |
| **文档状态** | 可落地开发 |
| **创建日期** | 2026-08-20 |
| **最近更新** | 2026-09-21 |

---

## 一、更新记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|----------|--------|
| V1.0 | 2026-08-20 | 初始版本：客户注册/登录、客户入驻申请（多模式流程模板框架 default/influencer/customer_koc/koc_lite）、模式选择弹窗、草稿/提交/审核状态机、版本历史、手工节点流转 | 产品部 |
| V1.1 | 2026-09-17 | ①新增「马卡乐合伙人」入驻模式：品牌固定为「马卡乐」、编辑页使用「企业信息+业务能力」专属字段集；②企业信息补充银行五字段（客户银行账号名称/银行账号/开户银行/开户支行/联行号） | 产品部 |
| V1.2 | 2026-09-18 | ①KOC 类「账号信息录入」中 UID图片、合作码图片升级为必填（提交拦截）；②达人流程编辑页隐藏「账号信息录入」模块；③「客户合作签约」菜单归入草稿目录 | 产品部 |
| V1.3 | 2026-09-21 | ①流程节点优化：default 模板删除「项目规划填写」→ 12 阶段；马卡乐合伙人启用专属 makale 模板 11 阶段；节点更名：网签合同→签署合作合同、规则宣导培训→签署品牌规则、BPM供应商创建→BPM审批建档；②新增申请弹窗：品牌改为复选框多选（马卡乐仅「马卡乐」默认勾选）且不展示入驻平台；「意向平台」改为文本输入；③账号信息录入新增待提交/已提交状态（已提交锁定编辑）与「导出 CSV」；④客户注册账号新增启用/禁用状态（注册默认启用，禁用后不可登录）；⑤客户合作管理删除「入驻类型」字段新增「品牌」列，提交唯一校验键改为「客户名称+品牌+入驻模式+入驻平台」；⑥节点流转入口收敛：入驻申请编辑页（达人/客户KOC、素人/联营KOC）去除「更新节点」按钮及手工流转弹窗，客户合作管理列表页同步去除行内「更新节点」链接与弹窗，手工流转仅在合作管理详情页保留；⑦新增注册成功页（register-success.html）：首次注册不再直进工作台全景图，落点专属成功页回显账号档案、明确「注册≠入驻」并预告四步流程，主 CTA 直达客户入驻申请；登录/演示快捷仍直进工作台 | 产品部 |

---

## 二、需求背景

### 2.1 业务背景

内容电商分销业务需要批量引入外部客户（代运营商、达人、机构、KOC、品牌合伙人）。客户从「注册账号」到「完成入驻」涉及：企业信息申报 → 运营审核 → BPM 建档 → 合同签署 → 保证金 → 授权书 → 平台账号录入 → 品牌规则签署等多个环节，且不同入驻模式的流程环节与申报材料差异大（如达人无企业审核链路、素人 KOC 轻量 6 节点、马卡乐合伙人另有专属申报材料）。

### 2.2 痛点分析

| 痛点 | 描述 |
|------|------|
| 流程不统一 | 各模式混用同一长流程，达人/KOC 被多余节点阻塞 |
| 材料反复补录 | 表单无法暂存，客户一次填不完即丢失 |
| 进度不透明 | 客户与运营均无法直观看到当前所处节点 |
| 线下环节无留痕 | BPM/合同/保证金等线下事项无审计记录 |
| 合伙人场景错配 | 马卡乐品牌合伙模式申报材料与通用四类字段完全不匹配 |

### 2.3 产品目标

- 建立**注册即建档**的客户账号体系（客户注册账号可被运营侧管理）
- 以**入驻模式驱动流程模板与表单字段集**：8 种模式 → 5 套流程模板（default 12 / makale 11 / influencer 8 / customer_koc 10 / koc_lite 6），页面按 flowKey 自动路由
- 提供**草稿自动保存 + 状态机（待提交/已提交/已审核）+ 版本历史**的申报体验
- 线下环节通过**受控手工节点流转**推进并强制审计备注
- 运营侧以**客户合作管理**台账统一跟踪全部已提交申请，客户侧以**账号信息录入**维护平台账号

### 2.4 预期收益

| 指标 | 现状 | 目标 |
|------|------|------|
| 客户资料一次录入完整率 | 低（无暂存） | 自动保存兜底，弃单率 < 10% |
| 入驻全流程平均时长 | 线下不可测 | 节点可视化，各环节可度量 |
| 线下事项可追溯性 | 无记录 | 100% 版本留痕（含流转备注） |
| 模式错配返工 | 常见 | 模式专属模板/字段集，零错配 |

---

## 三、业务流程

### 3.1 客户入驻总流程

```
客户注册/登录（pages/recruit/login.html，独立新标签）
   │ 手机号+姓名+对接商务+验证码 → 自动登录（talent 身份）
   ├─ 首次注册成功 → 注册成功页 register-success.html（回显档案+四步引导）
   │                      └─ 主 CTA「去发起客户入驻申请」→ manage.html
   └─ 登录/演示快捷成功 → 直进工作台 index.html
   ▼
工作台（index.html，客户视角）
   │
   ├─ 客户入驻申请（manage.html）
   │    新增申请 → 模式选择弹窗（8 模式 / 品牌多选 / 平台单选）
   │       ├─ 账号代运营/整店代运营/机构 → default 12 节点，标准四段表单
   │       ├─ 马卡乐合伙人 → makale 11 节点，专属两段表单（弹窗免选平台，品牌仅马卡乐）
   │       ├─ 达人 → influencer 8 节点（manage-influencer.html）
   │       ├─ 客户KOC → customer_koc 10 节点（manage-influencer.html）
   │       └─ 素人KOC/联营KOC → koc_lite 6 节点（manage-koc-lite.html）
   │    填写（编辑中，2s 静默自动保存）→ 提交（已提交）→ 运营审核（已审核·只读）
   │    → 后续节点手工流转：BPM审批建档 → 签署合作合同 → 保证金缴纳 → 授权书签署
   │      → 账号信息录入 →（内部软件开通）→ 签署品牌规则 → 入驻完毕
   │
   ├─ 账号信息录入（account-entry 三页，客户视角）：按模式双字段集录入平台账号
   │
   ▼
运营侧（工作台 · 运营视角）
   ├─ 客户注册账号（profile-list.html）：查看客户注册档案
   └─ 客户合作管理（recruit/list.html + detail.html）：
        已提交/已审核台账 → 明细查看 → 审核 / 更新节点 / 维护对接商务
```

### 3.2 申请单状态机

```
待提交(unsubmit) --提交(唯一性校验:客户名称+品牌+入驻模式+入驻平台)--> 已提交(submitted)
已提交 --运营审核(operator/admin)--> 已审核(reviewed，终态只读)
待提交/已提交：可编辑、可修改模式/品牌/平台（写入版本记录）
默认所处节点：unsubmit=3 填写企业信息；submitted=4 运营审核企业；reviewed=5 起始业务节点
（stageNo 显式流转值优先于状态推导；advanceStage 仅已审核、+1、备注必填）
```

---

## 四、需求描述

| 编号 | 功能 | 页面 | 视角 | 说明 |
|------|------|------|------|------|
| F1 | 客户注册/登录 | pages/recruit/login.html | 客户（独立页） | 手机号+验证码登录、注册自动登录、演示快捷登录 |
| F2 | 客户入驻申请 | manage.html 及两个模式流程页 | 客户/运营 | 模式弹窗、申请列表、动态表单、状态机、版本历史、节点流转 |
| F3 | 账号信息录入 | recruit/account-entry(-edit/-detail).html | 客户 | 主表+录入页+详情页三页体系，模式驱动双字段集 |
| F4 | 客户注册账号 | recruit/profile-list.html | 运营侧 | 客户注册档案固定 8 列台账 |
| F5 | 客户合作管理 | recruit/list.html + detail.html | 运营/BD/管理员 | 已提交申请台账 + 只读详情 + 审核 + 更新节点 |

通用约束：全部数据前端 localStorage 持久化（申请域 `onboarding_applications` 密文存储，XOR+Base64，前缀 `OBENC1:`；注册档案 `recruit_talents`；录入账号 `recruit_account_entries`；登录态 `recruit_current_phone`）；字典唯一数据源为 `js/onboarding-store.js`；页面均受 `Auth.requireAuth()` 守卫；≥1024px 表格视图、<1024px 卡片流。

---

## 五、需求列表

| 需求ID | 所属功能 | 需求点 | 优先级 |
|--------|----------|--------|--------|
| REQ-101 | F1 注册 | 注册（手机号+姓名+对接商务+验证码）成功即自动登录 | P0 |
| REQ-102 | F1 注册 | 登录（手机号+验证码），未注册引导去注册并回填手机号 | P0 |
| REQ-103 | F1 注册 | 演示快捷登录（跳过验证）登录固定演示客户 | P1 |
| REQ-104 | F1 注册 | 注册成功落点专属成功页（引导发起入驻）；登录/演示快捷跳转工作台 index.html（禁止跳不存在的 mine.html） | P0 |
| REQ-105 | F1 注册 | 注册成功页：回显姓名/手机号/账号编号/对接商务，含「注册≠入驻」认知引导与四步流程预告，主 CTA 直达客户入驻申请，次链接进工作台；需登录守卫 | P0 |
| REQ-201 | F2 入驻申请 | 模式选择弹窗：8 模式单选、品牌复选框多选（至少 1 项）、平台单选 | P0 |
| REQ-202 | F2 入驻申请 | 马卡乐合伙人：品牌仅「马卡乐」默认勾选；弹窗隐藏入驻平台行且免校验 | P0 |
| REQ-203 | F2 入驻申请 | 按 flowKey 路由编辑页并渲染对应流程节点条（default12/makale11/influencer8/customer_koc10/koc_lite6） | P0 |
| REQ-204 | F2 入驻申请 | 标准四段表单 / 马卡乐两段表单；月度分解目标 1-12 月网格 + 合计非阻塞提示 | P0 |
| REQ-205 | F2 入驻申请 | 2s 静默自动保存（不产生版本）；显式保存产生版本与变更字段清单 | P0 |
| REQ-206 | F2 入驻申请 | 提交唯一性校验（客户名称+品牌+入驻模式+入驻平台，客户名称含马卡乐 enterprise.entName 口径）；状态机与已审核后只读 | P0 |
| REQ-207 | F2 入驻申请 | 运营审核（operator/admin）：已提交→已审核，可附备注 | P0 |
| REQ-208 | F2 入驻申请 | 手工节点流转：仅已审核且未到末节点，+1，备注必填≤200字，写 kind=stage 版本；**入口唯一在「客户合作管理」详情页，入驻申请编辑页与合作管理列表页均不提供更新节点按钮** | P0 |
| REQ-209 | F2 入驻申请 | 营业执照上传模拟识别：自动回填企业全称/税务登记号/所在地（马卡乐仅回填企业名称） | P1 |
| REQ-210 | F2 入驻申请 | KOC 三模式编辑页账号信息录入：UID/昵称/合作码/UID图片/合作码图片必填，提交拦截；达人隐藏该模块 | P0 |
| REQ-211 | F2 入驻申请 | 流程页「修改入驻模式」：同流程原地重载结构，跨流程按 flowTarget 跳转对应页面 | P1 |
| REQ-301 | F3 账号录入 | 主表查询（昵称/UID/模式/主销品牌/合作码）+ 新增入口 + 行编辑/查看 | P0 |
| REQ-302 | F3 账号录入 | 录入页按入驻模式切换字段集（KOC 轻量 6 项 vs 直播账号完整 14 项） | P0 |
| REQ-303 | F3 账号录入 | 主销品牌→账号定位联动、运营模式字典（AO 编码，录入开放 10 项）、默认个人号/主账号 | P1 |
| REQ-304 | F3 账号录入 | 记录状态：新增默认待提交；列表可提交（二次确认）；已提交锁定不可编辑（列表隐藏编辑入口，直访 URL 加载/保存双重拦截）；详情页展示状态 | P0 |
| REQ-305 | F3 账号录入 | 导出当前列表全部字段为 CSV（UTF-8 BOM，兼容 Excel，无数据时提示不导出） | P1 |
| REQ-401 | F4 注册账号 | 运营侧固定列档案台账（含用户编号、对接商务、最近登录时间） | P0 |
| REQ-402 | F4 注册账号 | 账号状态启用/禁用：注册成功默认启用（历史无状态数据视为启用）；仅 operator/bd/admin 可切换（二次确认）；禁用后登录拦截提示；切换后保持当前筛选刷新 | P0 |
| REQ-501 | F5 合作管理 | 台账 11 列（含品牌、入驻节点、节点状态，已删除入驻类型），支持客户名称/品牌/平台/状态筛选与分页 | P0 |
| REQ-502 | F5 合作管理 | 只读详情：基本信息+全量表单卡片（含月度表、马卡乐两段）+操作日志（倒序分页 10 条） | P0 |
| REQ-503 | F5 合作管理 | 详情页审核入口（operator/admin + 已提交）与更新节点入口（operator/bd/admin + 已审核未到末节点） | P0 |

---

## 六、需求详情

### 6.1 F1 客户注册 / 登录（pages/recruit/login.html）

**入口**：工作台客户视角「客户注册」→ `window.open('pages/recruit/login.html?mode=register')` 新标签打开（带 `?mode=register` 默认展示注册 Tab）。

**双 Tab**：登录 / 注册，互斥切换；页面含登录提示「立即注册」「演示快捷登录（跳过验证）」。

| 场景 | 字段/规则 | 校验 |
|------|-----------|------|
| 注册 | 手机号 | `^1\d{10}$`，已注册提示「请直接登录」并切到登录 Tab 回填号码 |
| 注册 | 姓名、对接商务 | 非空必填 |
| 注册 | 验证码 | 固定演示码 1234，获取后 60s 倒计时重发 |
| 登录 | 手机号+验证码 | 手机号必须已注册，未注册 toast 引导注册并回填 |
| 演示快捷 | 固定账号 138****2231 / 李潮 / T_DEMO_001 | 不存在则补写档案，存在则刷新登录时间 |

**写入**：`recruit_talents[phone] = { phone, name, bdContact?, talent_id, status:'enabled', created_at, updated_at, last_login_at }`；会话 = `Auth.setCurrentUser({role:'talent',...})` + `recruit_current_phone`。
**成功后落点分场景**：注册 → toast 后 400ms 跳 `register-success.html`（同目录）；登录/演示快捷 → `../../index.html` 工作台。成功页（register-success.html，需登录守卫）回显姓名/手机号/账号编号/对接商务，展示四步入驻流程预告，主 CTA `../store-engine/manage.html`「去发起客户入驻申请」，次链接「先进入工作台看看」。页面 `<head>` 含 no-cache 响应头防旧脚本缓存。
**边界**：时间格式 `zh-CN` 24 小时制、`/`→`-`。

### 6.2 F2 客户入驻申请

#### 6.2.1 模式选择弹窗（新增申请 / 修改入驻模式共用）

- 模式卡片单选（radiogroup）8 项：账号代运营、整店代运营、达人、机构、联营KOC、素人KOC、客户KOC、马卡乐合伙人，附描述文案。
- **品牌：复选框胶囊多选**（至少 1 项，未选提示「请至少选择一个入驻品牌」）；选项随模式联动：马卡乐合伙人仅「马卡乐」且默认勾选，其余模式为巴拉/迷你/森马。多选结果以「、」连接存入 `brand` 字符串字段（列表/详情/唯一校验沿用）。
- **入驻平台：单选**（抖音/视频号/小红书/快手）；**选中马卡乐合伙人时整行隐藏且免校验**（平台意向改由编辑页「业务能力-意向平台」文本输入承载）。
- 新建确认 → `createDraft`（编号 `SQ+YYYYMMDD+4位流水`）并按 flowKey 路由；修改确认 → `updateMode`（记录模式/品牌/平台变更版本，已审核拒绝）。

#### 6.2.2 流程模板与路由（js/onboarding-store.js 唯一数据源）

| flowKey | 适用模式 | 阶段序列 | 编辑页 |
|---------|----------|----------|--------|
| default | 账号代运营/整店代运营/机构（历史 DP、达播机构） | 1 客户注册→2 选择入驻模式→3 填写企业信息→4 运营审核企业→5 BPM审批建档→6 签署合作合同→7 保证金缴纳→8 授权书签署→9 账号信息录入→10 内部软件开通→11 签署品牌规则→12 入驻完毕 | manage.html |
| makale | 马卡乐合伙人 | 同 default 去掉「内部软件开通」，共 11 阶段 | manage.html（专属两段表单） |
| influencer | 达人（历史达播达人） | 1 客户注册→2 选择入驻模式→3 供应商信息录入→4 BPM审批建档→5 签署合作合同→6 授权书签署→7 账号信息录入→8 入驻完毕 | manage-influencer.html |
| customer_koc | 客户KOC | influencer 基础上在第 7 后增「软件开通」「签署品牌规则」，共 10 阶段 | manage-influencer.html |
| koc_lite | 素人KOC/联营KOC | 1 客户注册→2 选择入驻模式→3 账号信息录入→4 内部软件开通→5 签署品牌规则→6 入驻完毕 | manage-koc-lite.html |

- 未知模式兜底 default；页面加载守卫按 flowKey 纠正路由（`location.replace(flowTarget(...))`）。
- 流程指示器一律 `Store.stagesForApp(app)` 动态渲染，禁止硬编码阶段数/名称。

#### 6.2.3 申请表单（manage.html）

**标准四段**（default）：企业业务能力（企业介绍*、经营规模、合作品牌信息、付费流量运营案例、内容打造案例）→ 企业负责人（负责人联系方式*、收件地址*）→ 项目规划（年度目标*万元、账号规划*、团队配置*、短视频内容策略*、投放策略*、场地设备计划* + 月度分解目标 1-12 月*）→ 财务信息（所在地*境内/境外（境外加街道必填）、企业全称*、企业简称*、税务登记号*、开户行五要素、纳税人类别*、账户持有人*、收款账号*）。

**马卡乐两段**（makale）：
- 企业信息（12 项）：企业与机构名称（营业执照）*、客户经营详细地址、客户简称、客户电话、客户联系人、联系人电话、纳税人类别（下拉 4 项）、客户银行账号名称、银行账号、开户银行、开户支行、联行号。
- 业务能力（20 项 + 月度）：渠道能力、**意向平台（文本输入）**、年度目标、月度分解目标（1-12 月，年度目标后渲染）、主营品类、货源情况、通货情况、款式开发能力、追单能力、货品价格、公司资金能力、授权历史、现有其他品牌、最好单店销售数据、单品牌平均单店销售数据、运营团队人数、视觉模式、付费费比、退货率、店铺模式、目前店铺链接。

**通用交互**：单页两段式滚动表单；字段变更 2s 防抖静默自动保存（不落版本）；显式保存/提交前合并；月度合计≠年度目标仅黄色警示不阻断；字段级行内错误提示；状态横幅（已提交=可继续修改说明；已审核=只读说明+审核备注）。

#### 6.2.4 模式流程页（manage-influencer / manage-koc-lite）

- 页头：返回 + 标题（随模式刷新）+ 保存/提交按钮（按状态显隐）。
- 区块：入驻信息卡（模式/品牌/平台 + 修改入驻模式按钮，跨流程自动跳转）→ 流程指示器 → 供应商信息录入（仅 influencer/customer_koc，财务字段与 default 同源 `FIELD_DEFS.finance`，含营业执照识别）→ 账号信息录入（多行）→ 操作日志（倒序分页）。
- **账号信息录入（KOC 字段集）**：每行含 账号UID*、账号昵称*、合作码*、对接商务（默认取 `recruit_talents` 本人档案，可覆盖）、**UID图片\*、合作码图片\***（JPG/PNG≤5MB，仅存文件名，图片会话内预览不落盘）。
- **提交必填校验**：逐行按序校验「账号N：请填写账号UID/账号昵称/合作码/请上传UID图片/请上传合作码图片」，未过 toast 拦截并阻断提交；草稿保存不拦截。
- **达人（influencer）**：整个「账号信息录入」面板隐藏（`accountsPanel` display:none），保存时 `form.accounts=[]`，不受必填影响。

#### 6.2.5 列表页（manage.html 列表视图）

查询区（关键词：编号/模式/品牌/申请人；状态筛选）+ 汇总 chips（全部/待提交/已提交/已审核）+ 10 列表格（序号、申请编号、入驻模式、品牌、入驻平台、申请人、入驻节点 X/N·名称、状态、更新时间、操作）+ 分页（页码压缩 1…c-1 c c+1…N，可切页大小）+ 行操作：查看（只读）/编辑（非已审核）/提交（待提交）/审核（operator|admin 且已提交）。客户角色仅见本人申请（`Auth.filterApplications`）。

#### 6.2.6 数据与版本

- 每次显式保存/提交/审核/改模式/流转追加版本 `{v, time, operator, action, changes[], snapshot}`，上限 30 条滚动。
- 落盘整体加密；读取损坏时按空列表兜底；旧四态数据（draft/pending/approved/rejected）读取时自动迁移三态。

### 6.3 F3 账号信息录入（recruit 三页，客户视角菜单）

- **主表**（account-entry.html）：查询区（账号昵称、账号UID、入驻模式▼、主销品牌▼、合作码 + 查询/重置 + 生效条件标签）；13 列表格（序号、入驻模式、账号昵称、账号UID、合作码、账号所属平台、账号运营模式、主销品牌、账号定位、账号负责人、创建时间、状态、操作）；移动端卡片流；新增按钮 → `account-entry-edit.html?mode=add`；**导出 CSV 按钮**（独立于新增）导出当前筛选结果全部列表字段（序号+11 数据列，含状态），UTF-8 BOM，文件名 `账号信息录入_yyyyMMddHHmmss.csv`，无数据 toast 提示不导出；模式/品牌下拉取自字典（`MODES`/`ACCOUNT_BRANDS`）。
- **记录状态**：`status: unsubmit 待提交（status-tag initial）/ submitted 已提交（status-tag submitted）`，无状态历史数据视为待提交；行操作：待提交显示「编辑/提交/查看」，已提交仅显示「查看」；提交需二次确认「提交后不可再编辑」，写入 updated_at。
- **录入页**（edit）：顶部「入驻模式」必选（编辑态锁定不可改）驱动字段集切换；新增保存默认 `status=unsubmit`；已提交记录直访编辑 URL 时加载与保存两道拦截（toast「已提交的账号信息不允许编辑」并返回列表）；
  - KOC（客户KOC/素人KOC/联营KOC）：账号UID、账号昵称、合作码、对接商务、UID图片上传、合作码图片上传；
  - 其它模式（直播账号完整集）：账号所属平台、账号昵称、账号运营模式（AO 字典录入开放 10 项）、账号UID、账号ID、账号类型（默认个人号）、账号负责人、账号绑定手机号、主销品牌、账号属性（默认主账号）、账号定位（随主销品牌联动：森马→男装/女装，巴拉巴拉→中童/幼童/婴童，迷你巴拉→婴童）、绑定店铺名称、账号注册主体、账号运营机构。
- **详情页**（detail）：按字段集分区只读展示，基本信息含「状态」标签行；KOC 展示凭证图片区；空值显示为空。
- 存储 `recruit_account_entries`，经 shared 模块（AppStorage/esc/toast）统一读写，页面不保留本地副本实现。

### 6.4 F4 客户注册账号（recruit/profile-list.html，仅运营侧）

- 侧边栏仅 operator/bd/admin 可见（置于「运营视角」分区首位）；数据源 `recruit_talents`；需登录。
- 列：序号、用户编号(talent_id)、姓名、手机号、对接商务、创建时间、更新时间、最近登录时间、**状态、操作**。
- **账号状态**：`status: enabled 启用（status-tag approved）/ disabled 禁用（status-tag cancelled）`；注册成功与演示建档默认启用，历史无状态数据视为启用。
- **启用/禁用切换**：仅 operator/bd/admin 展示入口（表格链接/卡片按钮），二次确认（禁用提示后果），写入 updated_at，切换后按当前筛选条件刷新；talent 仅可 viewing 自己档案无操作入口。
- **禁用生效**：入驻登记页（login.html）登录时拦截，toast「该账号已被禁用，请联系对接商务」；注册重复校验不受影响。

### 6.5 F5 客户合作管理（recruit/list.html + detail.html）

- **列表**：读取 `onboarding-store` 中已提交+已审核申请，按更新时间倒序；11 列：序号、客户名称（`finance.fullName || enterprise.entName`）、品牌（多选拼接原样展示）、入驻模式、入驻平台、对接商务、创建时间、创建人、更新人、更新时间、入驻节点（X/N·节点名徽章，运营侧另展示「已入驻完毕」终态提示）、节点状态；筛选：客户名称/品牌（下拉含巴拉/迷你/森马/马卡乐，多选品牌按包含匹配）/入驻平台/状态；移动端卡片（品牌与入驻模式分行展示）；行操作仅保留：明细（节点流转已收敛至详情页）、维护对接商务，列表页无手工流转弹窗。
- **详情（只读）**：流程进度条（按模式模板）+ 基本信息（申请编号、客户名称、当前节点、入驻模式、品牌、平台、对接商务、创建/更新/提交/审核时间、审核备注）+ 全量表单卡片（标准四段或马卡乐两段，月度分解表含合计）+ 操作日志（创建/编辑/提交/审核/流转统一时间倒序，分页 10 条）+ 审核按钮（operator|admin 且已提交）+ 更新节点按钮。
- **更新节点弹窗**：展示客户名称、当前→目标节点、警示说明（客户端可见节点变化不可见备注），备注必填≤200字，确认后写 kind=stage 版本并 toast 新节点名。

---

## 七、数据埋点

> 当前为纯前端演示环境，先按事件规范本地 console + 内存队列实现；接入后端后同 schema 上报。

**公共属性**：`user_id`（手机号脱敏/talent_id）、`role`、`app_id`（申请编号，如涉及）、`stage_no`、`ts`。

| 事件ID | 触发点 | 关键属性 | 用途 |
|--------|--------|----------|------|
| reg_submit | 点击注册 | phone_valid, has_bd | 注册转化 |
| reg_success / reg_fail | 注册结果 | fail_reason(dup/invalid_code/…) | 注册成功率 |
| rs_cta_click | 注册成功页点击主 CTA/工作台链接 | target(apply/workbench) | 注册→入驻启动转化 |
| login_success / login_fail | 登录结果 | entry(normal/demo), fail_reason(unreg) | 登录漏斗 |
| apply_create | 弹窗确认新建 | mode, brands[], platform, platform_skipped | 模式分布 |
| apply_modal_block | 弹窗校验拦截 | field(brand/platform), mode | 表单阻力分析 |
| apply_autosave / apply_save | 自动/显式保存 | duration_ms, changed_fields_count, result | 填写行为 |
| plan_month_mismatch | 月度合计≠年度目标 | sum, annual | 数据质量 |
| apply_submit / submit_dup_block | 提交/唯一拦截 | mode, brand | 提交漏斗 |
| review_pass | 运营审核完成 | has_remark, duration(提交→审核) | 审核时效 |
| stage_advance | 手工流转确认 | from, to, mode, reason_len | 环节时效（替代线下跟踪） |
| accounts_required_block | KOC 提交必填拦截 | missing(uid/nickname/code/uid_img/code_img) | 图片必填治理 |
| license_recognize | 执照识别回填完成 | mode, fields_filled | 功能使用率 |
| entry_submit | 账号信息录入保存 | mode, kind(koc/standard), account_count | 录入量 |
| entry_status_submit | 录入列表点击提交确认 | id, mode | 提交转化 |
| entry_export | 点击导出 CSV | row_count, filter_active | 导出使用率 |
| profile_status_toggle | 启用/禁用切换确认 | phone_hash, from, to, role | 账号治理监控 |
| login_blocked_disabled | 禁用账号登录被拦截 | phone_hash | 禁用生效监控 |
| coop_list_query / coop_detail_view | 台账查询/明细浏览 | filter_keys | 运营侧活跃度 |
| page_guard_redirect | requireAuth 拦截 | from_page | 越权访问监控 |

**漏斗定义**：注册成功 → 成功页 CTA 点击 → apply_create → apply_save → apply_submit → review_pass → stage_advance(至末节点)。

---

## 八、权限需求

| 功能点 | talent(客户) | operator(运营) | bd | admin(管理员) | 未登录 |
|--------|:---:|:---:|:---:|:---:|:---:|
| 客户注册/登录页 | ✅（独立页，无守卫） | ✅ | ✅ | ✅ | 可访问 |
| 客户入驻申请（列表/新增/编辑） | ✅ 仅本人（filterApplications） | ✅ 全部 | —（菜单不展示） | ✅ 全部 | → login.html |
| 提交审核 | ✅ 本人申请 | ✅ | — | ✅ | — |
| 运营审核 | ❌ | ✅ | ❌ | ✅ | — |
| 更新节点（手工流转） | ❌ | ✅ | ✅ | ✅ | — |
| 修改模式/品牌/平台、对接商务 | 已审核前可改 | ✅ | ✅ | ✅ | — |
| 账号信息录入（recruit 三页） | ✅ | ✅ | ✅ | ✅ | → login.html |
| 客户注册账号（profile-list） | ❌ 菜单不可见 | ✅ 含启用/禁用 | ✅ 含启用/禁用 | ✅ 含启用/禁用 | → login.html |
| 被禁用客户账号登录入驻登记 | ❌ 拦截 | — | — | — | — |
| 客户合作管理（BD 角色显示为「申请列表」） | ❌ | ✅ | ✅ | ✅ | → login.html |
| 首页页签 | ❌（客户账号无首页，流程页降级为普通页签） | ✅ | ✅ | ✅ | — |
| 草稿目录（开票信息录入/直播账号管理/保证金流水/客户合作签约/入驻流程/分销管理；运营侧含供应商申请入驻/客户合同管理等） | ✅ 客户版清单 | ✅ 运营版清单 | ✅ | ✅ | — |

角色判定统一走 `Auth.getCurrentUser().role` 与 `Auth.getRolePermissions(role)`；已审核申请对所有人只读（仅流转与查看）；备注类审计信息对 talent 不可见。

---

## 附录A 字典速查

- 入驻模式 8：账号代运营、整店代运营、达人、机构、联营KOC、素人KOC、客户KOC、马卡乐合伙人（历史兼容：DP、达播达人、达播机构）
- 申请品牌：巴拉、迷你、森马；马卡乐合伙人专属：马卡乐
- 平台：抖音、视频号、小红书、快手
- 主销品牌→账号定位：森马[男装,女装]、巴拉巴拉[中童,幼童,婴童]、迷你巴拉[婴童]
- 运营模式编码：AO+3位序号（共 13，录入开放 10）
- 状态：unsubmit 待提交 / submitted 已提交 / reviewed 已审核
