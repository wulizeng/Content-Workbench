# 产品需求文档（PRD）：详情页属性优化

| 项目 | 内容 |
|------|------|
| **产品名称** | 详情页属性优化 |
| **所属模块** | 包装优化 |
| **版本** | V1.5 |
| **文档状态** | 可落地开发 |
| **创建日期** | 2026-07-09 |

### 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| V1.0 | 2026-07-09 | 初始版本，包含列表视图、明细视图、商品详情页、导入核查、上传属性、编辑属性值、导出功能 |
| V1.1 | 2026-07-09 | 新增批次号追溯能力：批次号格式 PC+日期+4位序号，同一商品多次导入生成新记录（新批次），列表/明细/详情页均增加批次号列，明细视图增加批次号筛选 |
| V1.2 | 2026-07-10 | 新增上传属性校验逻辑（商品ID下所有未填属性必须全部填写属性值才允许上传）、删除操作状态校验（仅特定状态允许删除） |
| V1.3 | 2026-07-13 | 新增智能体中心模块：三栏布局（智能体卡片列表 / 智能体描述与流程说明 / 对话区），包含设置尺码参数智能体、商品推荐智能体、文本组件智能体 |
| V1.4 | 2026-07-13 | 智能体中心移除属性检查智能体，当前保留3个智能体 |
| V1.5 | 2026-07-15 | 架构重构：列表视图改为批次聚合+主子页面模式，检查状态新增「无异常/异常」语义，导入改为异步核查模式，属性视图编辑弹窗改为统一编辑所有未填属性 |

---

## 一、产品背景与目标

### 1.1 业务背景
内容电商平台要求商品详情页必须填写完整的必填属性（如产地、保质期、净含量、配料表等），否则会影响商品上架或搜索排名。运营人员需要批量检查商品的必填属性缺失情况，并补充上传缺失的属性值。

### 1.2 产品目标
- 支持**批量检查**商品必填属性缺失情况，支持异步核查模式
- 支持**属性维度**的属性值查看、统一编辑、上传
- 支持**增量合并**多次上传的属性数据
- 提供**双Tab+主子页面**架构：列表视图（批次聚合+商品明细子页面）+ 属性视图（属性维度）
- 支持**批次号追溯**，每次导入/上传分配独立批次号，便于操作追踪
- 检查状态语义化：无异常 / 异常 / 检查失败 / 未检查

---

## 二、用户角色

| 角色 | 权限 |
|------|------|
| 运营人员 | 导入核查、编辑属性、上传属性、导出 |
| 运营主管 | 全部权限 + 查看历史操作记录 |

---

## 三、功能架构

```
详情页属性优化
├── 列表视图 Tab
│   ├── 主列表（批次聚合）
│   │   ├── 流程说明区
│   │   ├── 筛选区（店铺编码 / 检查状态 / 创建时间）
│   │   ├── 工具栏（导入核查商品 / 导出 / 重新核查）
│   │   └── 批次统计表（店铺数 / 商品数 / 未检查 / 无异常 / 异常 / 检查失败 / 属性已上传 / 属性未上传 / 上传失败）
│   └── 明细子页面（点击「查看明细」进入）
│       ├── 筛选区（店铺编码 / 商品ID / 检查状态）
│       ├── 工具栏（导出 / 查看属性）
│       └── 商品明细表（店铺编码 / 店铺名称 / 商品ID / 商品名称 / 类目 / 检查状态 / 上传状态 / 失败原因）
├── 属性视图 Tab
│   ├── 筛选区（批次号 / 店铺编码 / 商品ID / 属性值 / 属性状态 / 失败原因）
│   ├── 工具栏（上传属性 / 导出明细）
│   └── 属性明细表（批次号 / 店铺编码 / 商品ID / 商品名称 / 属性名 / 属性值 / 上传状态 / 失败原因 / 更新时间 / 更新人）
├── 弹窗交互
│   ├── 导入核查商品弹窗（文本录入 / Excel导入）
│   ├── 上传属性弹窗（Excel上传）
│   └── 编辑未填属性弹窗（统一编辑该商品所有未填属性）
└── 智能体中心（独立页面）
```

---

## 四、页面结构与字段定义

### 4.1 列表视图（批次聚合 + 主子页面）

列表视图包含主列表和明细子页面，通过点击「查看明细」进入子页面，点击「返回列表」返回主列表。

#### 4.1.1 主列表 - 筛选区

| 筛选项 | 类型 | 说明 |
|--------|------|------|
| 店铺编码 | 文本输入 | 模糊匹配 |
| 检查状态 | 下拉选择 | 全部 / 未检查 / 无异常 / 异常 / 检查失败 |
| 创建时间 | 日期范围 | 起止日期筛选 |

- **查询**：按条件过滤列表
- **重置**：清空筛选条件，恢复全量展示
- 查询后展示**筛选标签**，可单个移除

#### 4.1.2 主列表 - 工具栏

| 按钮 | 功能 | 启用条件 |
|------|------|--------|
| 导入核查商品 | 弹出导入弹窗 | 始终可用 |
| 导出 | 导出已检查记录为Excel | 存在「无异常」或「异常」记录时启用 |
| 重新核查 | 对「未检查」记录触发异步核查 | 始终可用 |

#### 4.1.3 主列表 - 批次统计表

列表按批次号分组聚合展示，每行代表一个导入批次。

| 列名 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 复选框 | — | 交互 | 全选/单选 |
| 序号 | — | 数字 | 自增序号 |
| 批次号 | batchNo | 文本 | 等宽字体高亮，格式 PC+YYYYMMDD+4位序号 |
| 店铺数 | — | 数字 | 该批次包含的店铺数量 |
| 商品数 | — | 数字 | 该批次包含的商品数量 |
| 未检查 | — | 数字 | 灰色，checkStatus=“未检查”的商品数 |
| 无异常 | — | 数字 | 绿色，checkStatus=“无异常”的商品数 |
| 异常 | — | 数字 | 红色，checkStatus=“异常”的商品数 |
| 检查失败 | — | 数字 | 黄色，checkStatus=“检查失败”的商品数 |
| 属性已上传 | — | 数字 | 绿色，属性级别已上传数量 |
| 属性未上传 | — | 数字 | 灰色，属性级别未上传数量 |
| 上传失败 | — | 数字 | 红色，属性级别上传失败数量 |
| 创建人 | creator | 文本 | 批次内最早创建人 |
| 创建时间 | createTime | 日期时间 | 批次内最早创建时间 |
| 操作 | — | 链接 | 查看明细 / 删除 |

**操作列规则**：
- **查看明细**：点击进入该批次的商品明细子页面
- **删除**：删除整个批次的所有记录

#### 4.1.4 明细子页面

点击批次「查看明细」后，主列表隐藏，展示商品明细子页面。顶部有「返回列表」按钮。

**筛选区**：

| 筛选项 | 类型 | 说明 |
|--------|------|------|
| 店铺编码 | 文本输入 | 模糊匹配 |
| 平台商品ID | 文本输入 | 模糊匹配 |
| 检查状态 | 下拉选择 | 全部 / 未检查 / 无异常 / 异常 / 检查失败 |

**商品明细表**：

| 列名 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 复选框 | — | 交互 | 全选/单选 |
| 序号 | — | 数字 | 自增序号 |
| 店铺编码 | shopCode | 文本 | 等宽字体高亮 |
| 店铺名称 | shopName | 文本 | — |
| 平台商品ID | productId | 文本 | 等宽字体高亮 |
| 商品名称 | productName | 文本 | — |
| 类目名称 | categoryName | 文本 | — |
| 检查状态 | checkStatus | 状态标签 | 无异常 / 异常 / 检查失败 / 未检查 |
| 上传状态 | uploadStatus | 状态标签 | 已上传 / 未上传 / 上传失败 |
| 失败原因 | failReason | 文本 | 红色字体，无值时为空 |
| 创建时间 | createTime | 日期时间 | — |
| 创建人 | creator | 文本 | — |
| 操作 | — | 链接 | 查看属性 |

**操作列规则**：
- **查看属性**：跳转到属性视图 Tab，自动按批次+商品ID筛选

**空值处理**：所有无值的字段统一显示为空（不显示“—”或“未填写”）。

---

### 4.2 属性视图（属性维度）

#### 4.2.1 筛选区

| 筛选项 | 类型 | 说明 |
|--------|------|------|
| 批次号 | 文本输入 | 模糊匹配 |
| 店铺编码 | 文本输入 | 模糊匹配 |
| 平台商品ID | 文本输入 | 模糊匹配 |
| 属性值 | 文本输入 | 模糊匹配 |
| 属性状态 | 下拉选择 | 全部 / 未上传 / 已上传 / 上传失败 |
| 失败原因 | 文本输入 | 模糊匹配 |

#### 4.2.2 工具栏

| 按钮 | 功能 |
|------|------|
| 上传属性 | 弹出上传属性弹窗 |
| 导出明细 | 导出当前筛选条件下的属性明细为Excel |

#### 4.2.3 属性列表

| 列名 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 复选框 | — | 交互 | 全选/单选 |
| 序号 | — | 数字 | 自增序号 |
| 批次号 | batchNo | 文本 | 等宽字体高亮，格式 PC+YYYYMMDD+4位序号 |
| 店铺编码 | shopCode | 文本 | 等宽字体高亮 |
| 平台商品ID | productId | 文本 | 等宽字体高亮 |
| 商品名称 | productName | 文本 | — |
| 属性名 | attrName | 文本 | 属性名称 |
| 属性值 | attrValue | 文本 | 有值显示值，无值显示橙色「未填写」 |
| 上传状态 | uploadStatus | 状态标签 | 已上传 / 未上传 / 上传失败 |
| 失败原因 | failReason | 文本 | 红色字体，无值时为空 |
| 更新时间 | updateTime | 日期时间 | 属性级别，无值时为空 |
| 更新人 | updater | 文本 | 属性级别，无值时为空 |
| 操作 | — | 链接 | 补充属性 / 查看 |

**操作列规则**：
- **未上传** / **上传失败** → 显示「补充属性」，点击弹出统一编辑弹窗，展示该商品所有未填写属性
- **已上传** → 显示「查看」，点击查看属性信息

**数据来源**：遍历所有商品记录，展开其 `mockRequiredAttrs[productId]` 属性列表，每个属性一行。

---

### 4.3 商品属性详情页（已废弃）

> 原独立详情页已废弃，改为列表视图内嵌明细子页面 + 属性视图的方式实现。

---

## 五、核心功能详细设计

### 业务流程

```
导入核查商品 → 自动输出核查结果（异步） → 用户维护未填属性 → 上传详情页属性 → 结果查询
```

| 步骤 | 节点名称 | 说明 |
|------|---------|------|
| 1 | 导入核查商品 | 录入商品ID，创建记录（状态为「未检查」） |
| 2 | 自动输出核查结果 | 通过「重新核查」触发异步核查，系统生成检查结果（无异常/异常/检查失败） |
| 3 | 用户维护未填属性 | 在属性视图中，对未填属性进行统一编辑补充 |
| 4 | 上传详情页属性 | 导出空属性Excel模板，补充属性值后批量上传更新 |
| 5 | 结果查询 | 查看历史检查记录与属性更新结果，支持多维度筛选 |

### 5.1 导入核查商品

#### 5.1.1 弹窗结构

**弹窗标题**：导入核查商品

**两种录入模式**（Tab切换）：

| 模式 | 说明 |
|------|------|
| 文本录入 | textarea，支持英文逗号、中文逗号、换行分隔多个商品ID |
| Excel导入 | 上传 .xlsx / .xls 文件，自动识别商品ID列 |

**Excel导入**：
- 提供「下载导入模板」链接，生成模板文件
- 模板表头：`平台商品ID`、`店铺编码`（含示例数据行）
- 列识别关键词：`平台商品ID`、`商品ID`、`商品编号`、`ID`、`product_id`、`productId`
- 未匹配到关键词时默认取第一列

#### 5.1.2 检查流程

导入采用**异步核查模式**：导入时先创建记录（状态为「未检查」），后续通过「重新核查」按钮触发异步核查。

```
用户输入/上传商品ID列表
    ↓
点击「开始检查」
    ↓
创建记录，checkStatus = “未检查”
    ↓
分配批次号，写入列表
    ↓
Toast 提示导入完成
    ↓
用户点击「重新核查」触发异步核查
    ↓
系统调用平台接口检查必填属性
    ↓
根据检查结果更新状态：
  ├── 无异常：所有必填属性已填写
  ├── 异常：有必填属性未填写
  └── 检查失败：接口调用失败
```

#### 5.1.3 导入规则

| 情况 | 处理方式 |
|------|--------|
| 同一商品多次导入 | 每次均生成新记录，分配新批次号 |
| 商品ID在平台不存在 | 跳过，计入“未找到” |
| 商品ID导入成功 | 新增记录，检查状态=“未检查”，分配当前批次号 |

**Toast 提示格式**：`导入完成：新增 X 条（批次号：PC202607090002）`

#### 5.1.4 检查结果预览

弹窗底部展示预览表格：

| 列 | 说明 |
|----|------|
| 批次号 | 等宽字体高亮 |
| 店铺编码 | — |
| 店铺名称 | — |
| 商品ID | — |
| 空属性数 | 橙色高亮（>0），绿色（=0） |

---

### 5.1.5 重新核查（异步核查）

导入后记录状态为「未检查」，用户可通过「重新核查」按钮触发异步核查。

**触发条件**：列表中存在 `checkStatus = '未检查'` 的记录

**核查流程**：

```
用户点击「重新核查」
    ↓
筛选所有「未检查」记录
    ↓
逐条调用平台接口核查必填属性
    ↓
根据检查结果更新状态：
  ├── 无异常：所有必填属性已填写（emptyAttrs 为空）
  ├── 异常：有必填属性未填写（emptyAttrs 不为空）
  └── 检查失败：接口调用失败（记录 failReason）
    ↓
更新 checkTime、updateTime、updater
    ↓
刷新列表 + Toast 提示核查结果
```

**Toast 提示格式**：`核查完成：X 条已出结果，Y 条检查失败`

---

### 5.2 上传属性

#### 5.2.1 弹窗结构

**弹窗标题**：上传详情页属性

- 文件上传区（支持点击 + 拖拽）
- 格式限制：.xlsx / .xls
- 解析后展示数据预览（最多20行，超出显示"…共 N 条"）
- 「确认上传」按钮（文件解析成功后启用）

#### 5.2.2 增量合并规则

| 场景 | 处理方式 |
|------|---------|
| 上传的商品已存在于列表 | 更新上传状态、批次号和时间，保留原有属性值 |
| 上传的商品不在列表中 | 跳过，计入"未找到" |

**Toast 提示格式**：`上传完成：更新 X 条（批次号：PC202607090002）`

#### 5.2.3 属性级别增量合并

同一商品多次上传属性时，按属性名称匹配：

| 情况 | 处理方式 |
|------|---------|
| 属性名已存在 | 覆盖属性值，状态变为“已上传”，更新时间和更新人 |
| 属性名不存在 | 跳过（仅支持预定义属性列表内的属性） |

#### 5.2.4 上传执行校验

用户上传Excel并点击「确认上传」后，系统在**执行上传时**对每个商品ID进行完整性校验：

| 校验项 | 规则 | 提示 |
|--------|------|------|
| 未填属性完整性 | 该商品ID下所有状态为“未上传”的属性必须全部在Excel中提供属性值 | `商品 {productId} 存在 {N} 个未填属性未提供值，请补充后重新上传` |
| Excel属性覆盖度 | Excel中提供的属性必须覆盖该商品所有“未上传”状态的属性 | `商品 {productId} 缺少以下属性：{attrNames}` |

**执行流程**：

```
用户点击「确认上传」
    ↓
系统解析Excel内容
    ↓
按商品ID分组
    ↓
对每个商品ID执行校验：
  ├── 获取该商品所有“未上传”状态的属性列表
  ├── 检查Excel中是否包含所有这些属性
  ├── 检查每个属性的属性值是否非空
  └── 如校验失败，记录失败商品和缺失属性
    ↓
汇总校验结果
    ↓
如有失败：
  ├── Toast 显示失败汇总（如“3个商品校验失败”）
  ├── 弹窗展示详细失败列表（商品ID + 缺失属性）
  └── 阻止上传，不执行任何更新
如全部成功：
  └── 执行属性上传逻辑（按 5.2.2 / 5.2.3 规则处理）
```

**异常处理**：

| 场景 | 处理 |
|------|------|
| 商品ID下所有属性均为“已上传” | 跳过该商品，Toast 提示“商品 {productId} 无需上传” |
| Excel中包含“已上传”状态的属性 | 按 5.2.3 增量合并规则处理 |

---

### 5.3 编辑属性值（统一编辑弹窗）

#### 5.3.1 弹窗结构

**弹窗标题**：编辑未填属性

点击属性视图中的「补充属性」按钮，弹出统一编辑弹窗，展示该商品**所有未填写属性**。

| 区域 | 内容 |
|------|------|
| 商品信息栏 | 商品ID + 商品名称，未填写属性数量 |
| 属性列表 | 每个未填写属性一个输入框，支持滚动 |

**按钮**：取消 / 全部保存

#### 5.3.2 编辑权限规则

| 属性状态 | 操作 | 说明 |
|---------|------|------|
| 未上传 | 补充属性 | 弹出统一编辑弹窗，展示该商品所有未填写属性 |
| 上传失败 | 补充属性 | 同上 |
| 已上传 | 查看 | Toast 显示属性值，不可编辑 |

#### 5.3.3 保存逻辑

```
用户填写所有未填属性值
    ↓
点击「全部保存」
    ↓
非空校验（未填写的输入框标红提示）
    ↓
批量更新所有属性值
    ↓
记录 updateTime = 当前时间
    ↓
记录 updater = “当前用户”
    ↓
属性状态重置为“未上传”
    ↓
关闭弹窗 + 刷新视图 + Toast “已保存 X 个属性”
```

**空状态处理**：如果该商品所有属性已填写，点击「补充属性」时提示「该商品所有属性已填写」。

---

### 5.4 导出功能

#### 5.4.1 列表视图导出

- **按钮名**：导出
- **导出范围**：所有 `checkStatus = '无异常'` 或 `'异常'` 的记录
- **导出字段**：店铺编码、店铺名称、平台商品ID、商品名称、检查状态、检查时间、创建时间、创建人
- **文件名**：`详情页属性优化明细_YYYY-MM-DD.xlsx`

#### 5.4.2 明细视图导出

- **按钮名**：导出明细
- **导出范围**：当前筛选条件下的所有属性记录
- **导出字段**：店铺编码、店铺名称、平台商品ID、未填属性、属性值、上传状态、失败原因、创建时间、创建人
- **文件名**：`属性检查结果明细_YYYY-MM-DD.xlsx`

---

### 5.5 删除校验规则

#### 5.5.1 允许删除的状态

| 检查状态 | 是否允许删除 | 说明 |
|---------|------------|------|
| 未检查 | ✅ 允许 | 商品尚未核查，可直接删除 |
| 无异常 | ⚠️ 需二次确认 | 已核查无异常，删除后数据不可恢复 |
| 异常 | ⚠️ 需二次确认 | 已核查有异常，删除后数据不可恢复 |
| 检查失败 | ✅ 允许 | 检查过程失败，允许删除后重新导入 |

#### 5.5.2 删除前置校验

| 校验项 | 规则 | 提示 |
|--------|------|------|
| 属性上传状态 | 如该商品存在“已上传”状态的属性，需二次确认 | `商品 {productId} 已上传 {N} 个属性值，删除后不可恢复，确认删除？` |
| 批次关联性 | 如该批次下还有其他商品，提示用户 | `该批次下还有其他 {M} 个商品，是否一并删除？` |

#### 5.5.3 删除流程

```
用户点击“删除”
    ↓
检查商品状态
    ↓
未检查 / 检查失败：
  └── 弹出确认框：“确认删除商品 {productId}？”
无异常 / 异常：
  └── 检查是否有“已上传”属性
      ├── 无：弹出确认框：“确认删除商品 {productId}？删除后不可恢复”
      └── 有：弹出警告框：“商品 {productId} 已上传 {N} 个属性值，删除后所有属性数据将被清除，确认删除？”
    ↓
用户确认：
  ├── 是：执行删除，刷新列表，Toast 提示“删除成功”
  └── 否：取消操作
```

#### 5.5.4 批量删除

- 勾选多条记录后点击“批量删除”按钮
- 校验逻辑：对每条记录分别执行 5.5.2 校验
- 如有不满足删除条件的记录，弹窗展示失败列表，仅删除符合条件的记录
- Toast 提示：`成功删除 {X} 条，失败 {Y} 条`

---

## 六、数据模型

### 6.1 商品记录（列表视图）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Number | 记录唯一ID |
| batchNo | String | 批次号，格式 PC+YYYYMMDD+4位序号（如 PC202607090001） |
| shopCode | String | 店铺编码 |
| shopName | String | 店铺名称 |
| productId | String | 平台商品ID |
| productName | String | 商品名称 |
| checkStatus | Enum | 检查状态：未检查 / 无异常 / 异常 / 检查失败 |
| failReason | String | 失败原因 |
| checkTime | DateTime | 检查时间 |
| createTime | DateTime | 创建时间 |
| creator | String | 创建人 |
| uploadStatus | Enum | 上传状态：未上传 / 已上传 / 已完成 |
| uploadTime | DateTime | 上传时间 |
| updateTime | DateTime | 更新时间 |
| updater | String | 更新人 |
| emptyAttrs | Array | 空属性名称列表 |

### 6.2 属性记录（明细视图 / 详情页）

| 字段 | 类型 | 说明 |
|------|------|------|
| name | String | 属性名称（与 productId 组合为唯一标识） |
| value | String | 属性值 |
| uploadStatus | Enum | 未上传 / 已上传 / 上传失败 |
| failReason | String | 失败原因（仅上传失败时有值） |
| updateTime | DateTime | 属性级别更新时间 |
| updater | String | 属性级别更新人 |

### 6.3 唯一性约束

| 维度 | 唯一标识 | 说明 |
|------|---------|------|
| 列表视图 | id | 每条记录唯一，同一商品多次导入生成多条记录，通过批次号区分 |
| 明细视图 | productId + attrName | 同一商品下属性名唯一 |

### 6.4 批次号规则

| 项目 | 说明 |
|------|------|
| 格式 | PC + YYYYMMDD + 4位序号，如 PC202607090001 |
| 生成时机 | 每次导入核查、上传属性时自动生成新批次号 |
| 序号规则 | 同一日期内序号自增，从 0001 开始 |
| 同一商品多次导入 | 每次生成新记录，分配新批次号 |
| 上传属性 | 匹配已有记录，更新批次号为当前批次 |

---

## 七、状态枚举定义

### 7.1 检查状态（checkStatus）

| 枚举值 | 标签样式 | 说明 |
|--------|---------|------|
| 未检查 | 灰色 未检查 | 导入后待核查状态，等待异步核查 |
| 无异常 | 绿色 无异常 | 核查通过，所有必填属性已填写 |
| 异常 | 红色 异常 | 核查通过，但有必填属性未填写 |
| 检查失败 | 黄色 检查失败 | 核查过程出错，需记录 failReason |

**状态流转**：
```
导入 → 未检查 → (重新核查) → 无异常 / 异常 / 检查失败
```

### 7.2 属性状态（uploadStatus）

| 枚举值 | 标签样式 | 可编辑 |
|--------|---------|--------|
| 未上传 | 灰色 未上传 | ✅ 可编辑（通过「补充属性」统一编辑） |
| 已上传 | 绿色 已上传 | ❌ 仅查看 |
| 上传失败 | 红色 上传失败 | ✅ 可编辑（通过「补充属性」统一编辑） |

---

## 八、接口定义

### 8.1 批量检查必填属性

```
POST /api/product/check-required-attrs

请求参数：
{
  "productIds": ["P001", "P002", "P003"]
}

响应：
{
  "code": 0,
  "data": {
    "checked": [
      {
        "productId": "P001",
        "shopCode": "SH001",
        "shopName": "云途食品旗舰店",
        "productName": "经典坚果礼盒",
        "requiredAttrs": [
          { "name": "产地", "value": "", "required": true },
          { "name": "保质期", "value": "12个月", "required": true }
        ]
      }
    ],
    "notFound": ["P999"],
    "errors": [
      { "productId": "P006", "reason": "平台接口超时" }
    ]
  }
}
```

### 8.2 上传/更新商品属性

```
POST /api/product/update-attrs

请求参数：
{
  "productId": "P001",
  "attrs": [
    { "name": "产地", "value": "新疆" },
    { "name": "保质期", "value": "12个月" }
  ]
}

响应：
{
  "code": 0,
  "data": {
    "updated": 2,
    "failed": [],
    "errors": []
  }
}
```

### 8.3 导出检查结果

```
GET /api/product/export-check-result

请求参数：
?productIds=P001,P002&format=xlsx

响应：文件流（Excel）
```

---

## 九、交互规范

### 9.1 视图切换

- 列表视图 / 属性视图通过 **Tab 标签**切换
- 列表视图内包含主列表和明细子页面，通过「查看明细」/「返回列表」切换
- 切换到属性视图时自动刷新属性列表
- 切换到列表视图时自动返回主列表
- 切换不影响筛选条件

### 9.2 Toast 消息

| 类型 | 样式 | 自动消失 |
|------|------|---------|
| success | 绿色 | 2.5秒 |
| error | 红色 | 2.5秒 |
| info | 蓝色 | 2.5秒 |

### 9.3 弹窗交互

- 点击遮罩层关闭弹窗
- 点击关闭按钮（×）关闭弹窗
- 弹窗出入场动画（fade + slide）

### 9.4 空值显示规则

- 所有无值字段统一显示为**空**（不显示"—"或"未填写"）
- 状态标签列无值时不显示标签
- 失败原因列无值时为空

---

## 十、技术实现要点

### 10.1 数据存储

- **前端持久化**：localStorage（key: `package_optimizeList`）
- 列表数据和属性数据均通过 localStorage 实现跨页面共享
- 详情页通过 URL 参数 `?id=` 定位记录

### 10.2 页面通信

- 子页面通过 `window.parent.postMessage` 更新父框架面包屑
- 列表页与详情页通过 localStorage 共享数据

### 10.3 Excel 处理

- 使用 **SheetJS (xlsx)** 库
- 导入：解析 Excel 文件读取商品ID列表
- 导出：生成 Excel 文件并触发浏览器下载

### 10.4 页面结构

- 采用 **iframe 嵌套架构**：`index.html` 为主框架
- 列表页和详情页为独立 HTML 文件
- 全局样式依赖 `css/style.css`，页面内联局部样式

---

## 十一、异常处理

| 场景 | 处理方式 |
|------|--------|
| 导入的商品ID平台不存在 | 跳过并计入“未找到”，Toast 汇总提示 |
| 同一商品多次导入 | 每次均生成新记录，分配新批次号，状态为“未检查” |
| 上传属性的商品不在列表中 | 跳过并计入“未找到” |
| 编辑属性值保存时值为空 | 未填写的输入框标红，Toast 提示“请填写所有属性值” |
| Excel 文件格式错误 | Toast 提示“请上传 .xlsx 或 .xls 格式文件” |
| Excel 解析失败 | Toast 提示具体错误信息 |
| 重新核查时无待核查记录 | Toast 提示“没有待核查的记录” |
| 平台接口调用失败 | 记录检查状态=“检查失败”，填写失败原因 |

---

## 十二、智能体中心

### 12.1 模块概述

智能体中心为总入口页面，采用三栏布局设计，用户可通过左侧智能体卡片选择具体智能体进行对话式操作。

**侧边栏菜单名称**：详情页智能体

### 12.2 页面布局

```
智能体中心
├── 左侧：智能体卡片列表（240px）
│   ├── 设置尺码参数智能体（默认选中）
│   ├── 商品推荐智能体
│   └── 文本组件智能体
├── 中间：智能体描述与流程说明（340px）
│   ├── 智能体图标与名称
│   ├── 功能描述
│   ├── 核心能力标签
│   ├── 执行流程步骤
│   └── 开始对话按钮
└── 右侧：对话区（自适应）
    ├── 对话状态栏
    ├── 欢迎区（快捷提示）
    ├── 消息记录区
    └── 输入区（文件上传 + 文本输入 + 发送）
```

### 12.3 智能体列表

| 序号 | 智能体名称 | 功能说明 | 核心能力 |
|------|-----------|---------|----------|
| 1 | 设置尺码参数智能体 | 批量配置商品详情页尺码表中的重要参数和其他参数的勾选，支持对话输入或 Excel 导入 | 对话输入、Excel导入、批量配置、网页自动化、结果导出 |
| 2 | 商品推荐智能体 | 批量设置商品详情页中商品详情高级编辑下商品推荐组件及推荐关联商品并发布商品 | 推荐组件设置、关联商品配置、批量发布、Excel导入、结果导出 |
| 3 | 文本组件智能体 | 批量设置商品详情页中商品详情高级编辑下文本组件的编辑并自动发布 | 文本组件编辑、批量发布、Excel导入、操作类型选择、结果导出 |

### 12.4 智能体执行流程

#### 12.4.1 设置尺码参数智能体

| 步骤 | 节点名称 | 说明 |
|------|---------|------|
| 1 | 输入店铺商品信息 | 通过对话或上传 Excel 文件输入店铺及商品ID参数 |
| 2 | 智能体解析自动执行 | 智能体自动解析参数并调用平台接口执行配置 |
| 3 | 执行结果返回 | 返回店铺编码、商品数、成功数、失败数等结果 |

#### 12.4.2 商品推荐智能体

| 步骤 | 节点名称 | 说明 |
|------|---------|------|
| 1 | 输入店铺和平台商品ID | 选择智能体，在对话框输入店铺和平台商品ID，或者添加 Excel，并说明操作类型（增加或删除推荐商品） |
| 2 | 智能体自动执行 | 智能体自动设置商品推荐组件及推荐关联商品并发布 |
| 3 | 返回执行结果 | 智能体返回执行结果，含成功数、失败数等明细 |

#### 12.4.3 文本组件智能体

| 步骤 | 节点名称 | 说明 |
|------|---------|------|
| 1 | 输入店铺和平台商品ID | 选择智能体，在对话框输入店铺、平台商品ID和操作类型，或者添加 Excel |
| 2 | 智能体自动执行 | 智能体自动编辑文本组件并发布商品 |
| 3 | 返回执行结果 | 智能体返回执行结果，含成功数、失败数等明细 |

### 12.5 交互规范

- 点击左侧智能体卡片切换当前选中智能体
- 中间面板实时更新为选中智能体的描述和流程说明
- 右侧对话区重置为对应智能体的欢迎状态
- 支持文件上传（Excel、CSV、TXT、PDF、图片等）
- 对话消息支持用户/AI双方展示，含时间戳
- 执行结果以卡片形式嵌入对话消息中
