# 产品需求文档（PRD）：批量配置任务

| 项目 | 内容 |
|------|------|
| **产品名称** | 批量配置任务 |
| **所属模块** | 包装优化 |
| **版本** | V1.0 |
| **文档状态** | 可落地开发 |
| **创建日期** | 2026-07-22 |

### 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| V1.0 | 2026-07-22 | 初始版本：三Tab任务列表（尺码参数/商品推荐/文本组件）、新增/导入双入口创建批次、批次聚合统计、执行状态枚举、批次作废、批次明细页（基本信息/商品明细/操作日志）、商品明细条件删除 |

---

## 一、产品背景与目标

### 1.1 业务背景

内容电商运营中，商品详情页存在大量需要批量配置的场景：批量更新商品尺码参数、批量新增/删除商品推荐组件、批量编辑文本组件并发布。这些操作如果逐个商品手工执行，效率极低且容易出错。运营人员迫切需要一个统一的批量配置工作台，支持一次提交多个商品的配置任务，由系统自动执行并反馈结果。

### 1.2 产品目标

- 提供**统一批量配置入口**，覆盖三类配置任务：尺码参数、商品推荐、文本组件
- 支持**手动录入**与 **Excel 导入**两种商品录入方式，满足不同批量规模
- 以**批次**为管理单元，自动分配批次号，实现任务全生命周期追溯
- 提供**批次级聚合统计**（店铺数/商品数/成功数/失败数）与**执行状态自动推导**
- 提供**批次明细下钻**，查看每个商品的执行状态、失败原因、操作日志
- 支持对无效批次**作废**、对未执行/失败的商品明细**删除**，保证数据可控

---

## 二、用户角色

| 角色 | 权限 |
|------|------|
| 运营人员 | 新增任务、导入任务、查看明细、作废批次、删除商品明细、查询筛选 |
| 运营主管 | 全部权限 + 查看全部批次与操作日志 |

---

## 三、功能架构

```
批量配置任务
├── 页面标题区（标题 + 新增按钮 + 导入按钮）
├── 流程说明区（输入商品信息 → 系统自动执行 → 查看执行结果）
├── 查询区域（批次号 / 店铺编码 / 操作类型 / 执行状态 / 创建人 / 创建时间）
├── Tab 切换（胶囊卡片式）
│   ├── 尺码参数任务列表
│   ├── 商品推荐任务列表
│   └── 文本组件任务列表
├── 任务列表（按批次聚合）
│   ├── 批次统计表（店铺数 / 商品数 / 操作类型 / 执行状态 / 成功数 / 失败数 / 创建人 / 创建时间）
│   └── 操作列（查看明细 / 作废）
├── 弹窗交互
│   ├── 新增任务弹窗（任务类型 / 店铺编码 / 操作类型 / 平台商品ID 批量录入）
│   └── 导入任务弹窗（Excel 上传解析）
└── 批次明细页（点击「查看明细」新页签打开）
    ├── 基本信息（批次主表信息，一行4列）
    ├── 商品明细（商品行级状态 + 条件删除）
    └── 操作日志（流水式时间轴）
```

---

## 四、页面结构与字段定义

### 4.1 任务列表页

**页面路径**：`pages/package/size-params.html`
**侧边栏菜单**：包装优化 / 批量配置任务

#### 4.1.1 页面标题区

| 元素 | 说明 |
|------|------|
| 页面标题 | 批量配置任务 |
| 新增按钮 | 主按钮，点击弹出「新增任务」弹窗 |
| 导入按钮 | 次按钮，点击弹出「导入任务」弹窗 |

#### 4.1.2 流程说明区

以三步横向流程卡片展示，帮助用户理解功能闭环：

| 步骤 | 标题 | 说明 |
|------|------|------|
| 1 | 输入商品信息 | 填写店铺编码、商品ID及操作类型 |
| 2 | 系统自动执行 | 工作台自动执行批量配置参数 |
| 3 | 查看执行结果 | 查看成功数、失败数等明细 |

#### 4.1.3 查询区域

查询区域位于流程说明与 Tab 之间，**全局共享**，作用于所有 Tab。

| 查询项 | 类型 | 匹配规则 |
|--------|------|----------|
| 批次号 | 文本输入 | 模糊匹配 |
| 店铺编码 | 文本输入 | 批次内任一商品明细匹配即命中 |
| 操作类型 | 下拉选择 | 全部 / 更新 / 新增 / 删除，精确匹配 |
| 执行状态 | 下拉选择 | 全部 / 待执行 / 执行中 / 部分成功 / 全部成功 / 全部失败，按聚合后状态精确匹配 |
| 创建人 | 文本输入 | 模糊匹配 |
| 创建时间 | 日期范围 | 起止日期区间过滤（yyyy-MM-dd） |

- **查询**：按条件过滤当前 Tab 列表
- **重置**：清空全部查询条件，恢复全量展示
- 切换 Tab 时自动按「任务类型 + 查询条件」双重过滤

#### 4.1.4 Tab 切换

| Tab | 任务类型标识 | 说明 |
|-----|------------|------|
| 尺码参数 | size | 批量更新商品尺码表参数 |
| 商品推荐 | recommend | 批量新增/删除商品推荐组件 |
| 文本组件 | text | 批量新增/删除文本组件 |

- 采用**胶囊卡片式**设计：底槽容器 + 选中态紫蓝渐变填充 + 未选中态透明底
- 默认选中「尺码参数」Tab

#### 4.1.5 任务列表（批次聚合表）

每个 Tab 对应一个任务列表，按批次维度聚合展示，每行代表一个批次。

| 列名 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 序号 | — | 数字 | 自增序号 |
| 批次号 | batchNo | 文本 | 等宽字体高亮，格式见 6.3 |
| 店铺数 | — | 数字 | 批次内店铺编码去重统计 |
| 商品数 | — | 数字 | 批次内商品明细总数 |
| 操作类型 | opType | 文本 | 更新 / 新增 / 删除 |
| 执行状态 | — | 状态标签 | 由商品明细聚合推导，见 7.1 |
| 成功数 | — | 数字 | 绿色，状态为「已完成」的商品数 |
| 失败数 | — | 数字 | 红色，状态为「失败」的商品数 |
| 创建人 | creator | 文本 | 批次创建人 |
| 创建时间 | createTime | 日期时间 | 批次创建时间 |
| 操作 | — | 按钮 | 查看明细 / 作废 |

**操作列规则**：
- **查看明细**：始终显示，点击在系统内新页签打开批次明细页
- **作废**：仅当批次执行状态为「待执行」或「全部失败」时显示（红色危险按钮）

**空状态**：无数据时显示「暂无数据」。

---

### 4.2 批次明细页

**页面路径**：`pages/package/size-params-detail.html?batchNo={批次号}`
**打开方式**：列表页点击「查看明细」，通过系统页签机制（postMessage openTab）在系统内新页签打开；独立访问时降级为 window.open。

页面由三个区块组成，自上而下排列。

#### 4.2.1 基本信息区

展示批次主表信息，采用**一行4列**网格布局，字段以「字段名称：字段值」形式平铺展示。

| 字段 | 说明 |
|------|------|
| 批次号 | 等宽字体高亮 |
| 任务类型 | 尺码参数 / 商品推荐 / 文本组件 |
| 操作类型 | 更新 / 新增 / 删除 |
| 执行状态 | 状态标签，聚合推导，见 7.1 |
| 店铺数 | 批次内店铺去重数 |
| 商品数 | 批次内商品总数 |
| 成功数 | 绿色 |
| 失败数 | 红色 |
| 创建人 | 批次创建人 |
| 创建时间 | 批次创建时间 |

#### 4.2.2 商品明细区

以表格展示批次内每个商品行的执行明细。区块头部显示商品总数徽标（共 N 条商品）。

| 列名 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 序号 | — | 数字 | 自增序号 |
| 店铺编码 | shopCode | 文本 | 等宽字体高亮 |
| 平台商品ID | productId | 文本 | 等宽字体高亮 |
| 执行状态 | status | 状态标签 | 商品行级状态，见 7.2 |
| 失败原因 | failReason | 文本 | 红色字体，无值时显示「-」 |
| 更新时间 | updateTime | 日期时间 | 商品行级更新时间，无值时显示「-」 |
| 更新人 | updater | 文本 | 商品行级更新人，无值时显示「-」 |
| 操作 | — | 链接 | 删除 |

**操作列规则**：
- **删除**：仅当商品行状态为「待执行」或「失败」时显示（红色链接），点击二次确认后删除该商品明细
- 其他状态（执行中 / 已完成）显示「-」，不可删除

**空状态**：无商品明细时显示「暂无商品明细」。

#### 4.2.3 操作日志区

以**流水式时间轴**记录批次全生命周期操作，每条日志包含：操作动作、操作人、时间、详情描述。

| 日志节点 | 触发条件 | 详情内容 |
|---------|---------|---------|
| 创建批次 | 批次创建时 | 创建批次 {batchNo}（{任务类型} / {操作类型}），共 N 个商品、M 个店铺 |
| 加入执行队列 | 状态为待执行 | 批次已加入执行队列，等待调度执行 |
| 开始执行 | 开始执行时 | 开始执行「{操作类型}」操作 |
| 执行成功 | 每个成功的商品 | {shopCode} / {productId}「{操作类型}」成功 |
| 执行失败 | 每个失败的商品 | {shopCode} / {productId}：{失败原因} |
| 执行中 | 状态为执行中 | 已处理 X / N 个商品，任务执行中 |
| 执行完成 | 全部执行完毕 | 批次执行完成：成功 X 个，失败 Y 个 |

---

## 五、核心功能详细设计

### 5.1 新增任务（手动录入）

#### 5.1.1 弹窗结构

**弹窗标题**：新增任务

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 任务类型 | 下拉选择 | ✅ | 尺码参数 / 商品推荐 / 文本组件，默认取当前 Tab |
| 店铺编码 | 文本输入 | ✅ | 单个店铺编码 |
| 操作类型 | 下拉选择 | ✅ | 随任务类型联动，见 5.1.2 |
| 平台商品ID | 多行文本 | ✅ | 支持批量录入，英文逗号或换行分隔 |

#### 5.1.2 操作类型联动规则

| 任务类型 | 可选操作类型 |
|---------|------------|
| 尺码参数 | 更新 |
| 商品推荐 | 新增 / 删除 |
| 文本组件 | 新增 / 删除 |

切换任务类型时，操作类型下拉选项自动刷新。

#### 5.1.3 提交流程

```
用户填写任务类型、店铺编码、操作类型、平台商品ID
    ↓
点击「提交」
    ↓
校验：店铺编码非空、商品ID非空
    ↓
按英文逗号/换行分隔解析商品ID列表（去空）
    ↓
校验：至少解析到 1 个商品ID
    ↓
为每个商品ID生成商品明细（状态=待执行，更新时间/更新人为空）
    ↓
创建批次（生成批次号，记录任务类型/操作类型/创建人/创建时间）
    ↓
写入存储 + 刷新列表 + Toast「批次 {batchNo} 创建成功」
    ↓
关闭弹窗
```

---

### 5.2 导入任务（Excel 导入）

#### 5.2.1 弹窗结构

**弹窗标题**：导入任务

- 文件上传区（点击选择文件），支持 .xlsx / .xls
- 解析成功后展示文件信息条：`{文件名}（{任务类型} / {操作类型}，{N} 个店铺，{M} 条商品）`，支持「移除」重新选择

#### 5.2.2 Excel 模板字段

| 列名 | 必填 | 说明 |
|------|------|------|
| 店铺编码 | ✅ | 支持别名：店铺编码 / 店铺代码 / 店铺 / shop_code / shopCode |
| 平台商品ID | ✅ | 支持别名：平台商品ID / 商品ID / 商品编号 / product_id / productId；未匹配时默认取第一列 |
| 任务类型 | ✅ | 支持别名：任务类型 / 类型 / task_type / taskType；取值：尺码参数 / 商品推荐 / 文本组件 |
| 操作类型 | ✅ | 支持别名：操作类型 / 操作 / op_type / opType；取 Excel 原始值 |

#### 5.2.3 解析规则

| 规则项 | 说明 |
|--------|------|
| 任务类型/操作类型取值 | 取 Excel **首行**对应列的值，作为整个批次的任务类型与操作类型 |
| 任务类型映射 | 尺码参数→size，商品推荐→recommend，文本组件→text；无法识别时提示并阻断 |
| 商品明细构建 | 逐行读取「店铺编码 + 平台商品ID」，两列均有值的行才生成商品明细 |
| 商品明细初始状态 | 状态=待执行，更新时间/更新人为空 |

#### 5.2.4 导入流程

```
用户点击「导入」按钮
    ↓
弹出导入弹窗，选择 Excel 文件
    ↓
校验文件格式（.xlsx / .xls）
    ↓
解析 Excel（SheetJS），识别各列
    ↓
校验必需列：店铺编码 / 任务类型 / 操作类型（缺失则提示并阻断）
    ↓
逐行构建商品明细，统计店铺数
    ↓
校验任务类型可识别
    ↓
展示文件信息条（文件名 + 任务类型 + 操作类型 + 店铺数 + 商品数）
    ↓
点击「导入」确认
    ↓
创建批次（同 5.1.3 后半段）
    ↓
写入存储 + 刷新列表 + Toast 提示 + 关闭弹窗
```

---

### 5.3 批次聚合与执行状态推导

#### 5.3.1 聚合指标

| 指标 | 计算规则 |
|------|---------|
| 店铺数 | 批次内商品明细的 shopCode 去重计数 |
| 商品数 | 批次内商品明细总数 |
| 成功数 | 商品状态为「已完成」的数量 |
| 失败数 | 商品状态为「失败」的数量 |

#### 5.3.2 执行状态推导规则（批次级）

按以下优先级自上而下判断：

| 优先级 | 条件 | 推导状态 |
|--------|------|---------|
| 1 | 存在「执行中」的商品 | 执行中 |
| 2 | 全部商品均为「失败」（失败数 = 商品数 且 商品数 > 0） | 全部失败 |
| 3 | 存在「失败」的商品（有成功有失败） | 部分成功 |
| 4 | 全部商品均为「已完成」（商品数 > 0） | 全部成功 |
| 5 | 其他（均为待执行） | 待执行 |

---

### 5.4 查看批次明细

- 列表页点击「查看明细」，在**系统内新页签**打开批次明细页（通过 `window.parent.postMessage({type:'openTab', ...})` 通知主框架）
- 明细页通过 URL 参数 `batchNo` 定位批次数据
- 明细页「关闭」按钮通知主框架关闭当前页签（`postMessage({type:'closeTab'})`）；独立访问时降级为 window.close

---

### 5.5 作废批次

| 项目 | 规则 |
|------|------|
| 入口 | 列表页操作列「作废」按钮（红色危险按钮） |
| 显示条件 | 批次执行状态为「待执行」或「全部失败」 |
| 交互 | 点击后二次确认：`确定作废批次 {batchNo} 吗？作废后该批次将被移除。` |
| 处理逻辑 | 确认后从批次列表中移除该批次（含全部商品明细），持久化并刷新列表 |
| 结果提示 | Toast「批次 {batchNo} 已作废」 |

---

### 5.6 删除商品明细

| 项目 | 规则 |
|------|------|
| 入口 | 批次明细页商品明细操作列「删除」链接（红色） |
| 显示条件 | 商品行状态为「待执行」或「失败」 |
| 交互 | 点击后二次确认：`确定删除该商品明细吗？` |
| 处理逻辑 | 确认后从批次的商品明细列表中移除该行，持久化并重新渲染基本信息、商品明细、操作日志 |
| 联动影响 | 删除后批次的聚合指标（商品数/成功数/失败数）与执行状态自动重新推导 |

---

### 5.7 查询筛选

- 查询区域为全局共享，作用于全部三个 Tab
- 点击「查询」按当前条件刷新所有 Tab 列表；点击「重置」清空条件并刷新
- 筛选为「任务类型 + 查询条件」双重过滤：先按 Tab 类型过滤，再按查询条件过滤
- 执行状态筛选基于**聚合后**的批次状态进行精确匹配

---

## 六、数据模型

### 6.1 批次记录（batch）

| 字段 | 类型 | 说明 |
|------|------|------|
| batchNo | String | 批次号，唯一标识，格式见 6.3 |
| type | Enum | 任务类型：size / recommend / text |
| opType | Enum | 操作类型：更新 / 新增 / 删除 |
| creator | String | 创建人 |
| createTime | DateTime | 创建时间（yyyy-MM-dd HH:mm:ss） |
| items | Array | 商品明细列表，见 6.2 |

### 6.2 商品明细记录（item）

| 字段 | 类型 | 说明 |
|------|------|------|
| shopCode | String | 店铺编码 |
| productId | String | 平台商品ID |
| status | Enum | 商品行级执行状态：待执行 / 执行中 / 已完成 / 失败 |
| failReason | String | 失败原因（仅失败时有值） |
| updateTime | DateTime | 商品行级更新时间（待执行时为空） |
| updater | String | 商品行级更新人（待执行时为空） |

### 6.3 批次号规则

| 项目 | 说明 |
|------|------|
| 格式 | 2位任务类型前缀 + YYYYMMDD + 4位流水号，如 `SP202607220001` |
| 前缀映射 | SP=尺码参数，RC=商品推荐，TC=文本组件 |
| 流水号规则 | 同日同前缀下自增，从 0001 开始，自动补零至4位；不同前缀或不同日期独立计数 |
| 生成时机 | 新增任务或导入任务创建批次时自动生成 |

### 6.4 唯一性约束

| 维度 | 唯一标识 | 说明 |
|------|---------|------|
| 批次 | batchNo | 批次号全局唯一 |
| 商品明细 | batchNo + shopCode + productId | 批次内商品行定位 |

---

## 七、状态枚举定义

### 7.1 执行状态（批次级，聚合推导）

| 枚举值 | 标签样式 | 推导条件 |
|--------|---------|---------|
| 待执行 | 黄色 | 商品均为待执行 |
| 执行中 | 蓝色 | 存在执行中的商品 |
| 部分成功 | 橙色 | 有成功有失败 |
| 全部成功 | 绿色 | 全部商品已完成 |
| 全部失败 | 红色 | 全部商品失败 |

### 7.2 执行状态（商品行级）

| 枚举值 | 标签样式 | 说明 |
|--------|---------|------|
| 待执行 | 黄色 | 任务已创建，等待执行 |
| 执行中 | 蓝色 | 正在执行配置操作 |
| 已完成 | 绿色 | 配置操作执行成功 |
| 失败 | 红色 | 配置操作执行失败，记录失败原因 |

**状态流转（商品行级）**：
```
创建 → 待执行 → 执行中 → 已完成 / 失败
```

### 7.3 操作类型（opType）

| 任务类型 | 可选操作类型 |
|---------|------------|
| 尺码参数（size） | 更新 |
| 商品推荐（recommend） | 新增 / 删除 |
| 文本组件（text） | 新增 / 删除 |

---

## 八、接口定义

> 以下为前后端分离落地时的建议接口设计，当前 Mock 版本通过 localStorage 模拟。

### 8.1 分页查询批次列表

```
GET /api/batch-config/batches

请求参数：
?type=size&batchNo=&shopCode=&opType=&status=&creator=&createTimeStart=&createTimeEnd=&page=1&pageSize=20

响应：
{
  "code": 0,
  "data": {
    "total": 7,
    "list": [
      {
        "batchNo": "SP202607200001",
        "type": "size",
        "opType": "更新",
        "shopCount": 2,
        "productCount": 5,
        "status": "部分成功",
        "successCount": 4,
        "failCount": 1,
        "creator": "张三",
        "createTime": "2026-07-20 09:30:00"
      }
    ]
  }
}
```

### 8.2 创建批次（新增/导入）

```
POST /api/batch-config/batches

请求参数：
{
  "type": "size",
  "opType": "更新",
  "items": [
    { "shopCode": "SH001", "productId": "DY-20260701-001" }
  ]
}

响应：
{
  "code": 0,
  "data": { "batchNo": "SP202607220001" }
}
```

### 8.3 查询批次明细

```
GET /api/batch-config/batches/{batchNo}

响应：
{
  "code": 0,
  "data": {
    "batchNo": "SP202607200001",
    "type": "size",
    "opType": "更新",
    "creator": "张三",
    "createTime": "2026-07-20 09:30:00",
    "items": [
      {
        "shopCode": "SH001",
        "productId": "DY-20260701-001",
        "status": "已完成",
        "failReason": "",
        "updateTime": "2026-07-20 09:32:00",
        "updater": "张三"
      }
    ]
  }
}
```

### 8.4 作废批次

```
DELETE /api/batch-config/batches/{batchNo}

响应：
{ "code": 0, "data": true }
```

### 8.5 删除商品明细

```
DELETE /api/batch-config/batches/{batchNo}/items

请求参数：
{ "shopCode": "SH001", "productId": "DY-20260701-003" }

响应：
{ "code": 0, "data": true }
```

---

## 九、交互规范

### 9.1 Tab 切换

- 三个任务类型 Tab 采用胶囊卡片式设计，选中态紫蓝渐变填充、白字、投影
- 切换 Tab 仅切换列表面板，不影响查询条件
- 默认选中「尺码参数」Tab

### 9.2 弹窗交互

- 点击遮罩层关闭弹窗
- 点击关闭按钮（×）关闭弹窗
- 弹窗出入场动画（fade + slide）
- 必填字段未填写时 Toast 提示，阻断提交

### 9.3 Toast 消息

| 场景 | 提示文案 |
|------|---------|
| 创建成功 | 批次 {batchNo} 创建成功 |
| 作废成功 | 批次 {batchNo} 已作废 |
| 店铺编码为空 | 请输入店铺编码 |
| 商品ID为空 | 请输入平台商品ID |
| 未解析到商品ID | 未解析到商品ID |
| 文件格式错误 | 请选择 .xlsx 或 .xls 格式文件 |
| 缺少必需列 | 未找到"{列名}"列，请检查表头 |
| 任务类型无法识别 | 任务类型"{值}"无法识别，请填写：尺码参数/商品推荐/文本组件 |

### 9.4 二次确认

| 操作 | 确认文案 |
|------|---------|
| 作废批次 | 确定作废批次 {batchNo} 吗？作废后该批次将被移除。 |
| 删除商品明细 | 确定删除该商品明细吗？ |

### 9.5 空值显示规则

- 商品明细的失败原因、更新时间、更新人无值时显示「-」
- 操作列不可操作时显示「-」
- 列表无数据时显示「暂无数据」；商品明细为空时显示「暂无商品明细」

---

## 十、技术实现要点

### 10.1 数据存储

- **前端持久化**：localStorage（key: `size_params_batches`），存储批次及商品明细的完整 JSON 结构
- 列表页与明细页通过 localStorage 共享数据
- 列表页加载时优先读取 localStorage，无缓存时写入默认 Mock 数据
- 页面 `pageshow` 事件时重新加载数据并刷新，保证从明细页返回后数据同步

### 10.2 页面通信

- 明细页通过 `window.parent.postMessage({type:'openTab'/'closeTab'})` 与主框架（index.html）通信，实现系统内页签的打开与关闭
- 列表页通过 postMessage 更新主框架面包屑（包装优化 / 批量配置任务）

### 10.3 Excel 处理

- 使用 **SheetJS (xlsx)** 库解析导入文件
- 列识别采用别名候选数组匹配，未匹配商品ID列时默认取第一列

### 10.4 页面结构

- 采用 **iframe 嵌套架构**：`index.html` 为主框架
- 列表页（size-params.html）与明细页（size-params-detail.html）为独立 HTML 文件
- 全局样式依赖 `css/style.css`，页面内联局部样式

---

## 十一、异常处理

| 场景 | 处理方式 |
|------|--------|
| 新增时店铺编码为空 | Toast「请输入店铺编码」，阻断提交 |
| 新增时商品ID为空 | Toast「请输入平台商品ID」，阻断提交 |
| 商品ID解析结果为空 | Toast「未解析到商品ID」，阻断提交 |
| 导入文件格式非 Excel | Toast「请选择 .xlsx 或 .xls 格式文件」 |
| 导入文件为空 | Toast「Excel 文件为空」 |
| 导入缺少必需列 | Toast「未找到"{列名}"列，请检查表头」，阻断导入 |
| 导入任务类型无法识别 | Toast 提示合法取值范围，阻断导入 |
| 导入未解析到商品明细 | Toast「未解析到商品明细」，阻断导入 |
| Excel 解析异常 | Toast「文件解析失败，请检查格式」 |
| 明细页批次号不存在 | 隐藏内容区，展示空状态「未找到该批次数据」 |
| 作废/删除操作 | 均需二次确认，确认后持久化并刷新视图 |
